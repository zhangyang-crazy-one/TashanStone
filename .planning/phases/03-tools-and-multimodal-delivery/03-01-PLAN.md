---
phase: 03-tools-and-multimodal-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/assistant-runtime/types.ts
  - src/services/assistant-runtime/toolMediaContracts.ts
  - src/services/assistant-runtime/index.ts
  - test/services/assistantToolRuntimeContracts.test.ts
  - test/services/assistantDeliveryPolicyContracts.test.ts
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
must_haves:
  truths:
    - "Phase 3 starts from one explicit contract surface for tool execution, multimodal normalization, and delivery policy instead of growing ad hoc callback payloads."
    - "The shared runtime exposes typed seams that later plans can implement without guessing event shapes or attachment semantics."
    - "Delivery policy remains transport-neutral and does not hardcode WhatsApp or QQ Channel assumptions."
  artifacts:
    - path: "src/services/assistant-runtime/toolMediaContracts.ts"
      provides: "Canonical contract surface for tool execution inputs/results, multimodal media payloads, and delivery units"
    - path: "test/services/assistantToolRuntimeContracts.test.ts"
      provides: "Regression coverage for the shared runtime-facing tool and media contracts"
  key_links:
    - from: "src/services/assistant-runtime/toolMediaContracts.ts"
      to: "src/services/assistant-runtime/types.ts"
      via: "Phase 3 extends the existing runtime contract surface instead of creating a second model"
      pattern: "Assistant(Runtime|Tool|Attachment|Delivery)"
    - from: "src/services/assistant-runtime/index.ts"
      to: "test/services/assistantDeliveryPolicyContracts.test.ts"
      via: "The barrel export surface must expose delivery-policy contracts before downstream plans implement them"
      pattern: "delivery|tool|media"
---

<objective>
Define the Phase 3 contract surface first so tool execution, multimodal normalization, and outbound delivery all build against one shared runtime model.

Purpose: satisfy the schema and planning side of `TOOL-01`, `TOOL-02`, and `TOOL-03` before implementation work begins.
Output: shared tool/media/delivery contract files plus contract tests that lock in the transport-neutral shape.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-tools-and-multimodal-delivery/03-RESEARCH.md
@.planning/phases/03-tools-and-multimodal-delivery/03-VALIDATION.md
@src/services/assistant-runtime/types.ts
@src/services/assistant-runtime/index.ts
@src/services/assistant-runtime/createAssistantRuntime.ts
@src/services/assistant-runtime/providerExecution.ts

<interfaces>
From `src/services/assistant-runtime/types.ts`:
```typescript
export interface AssistantNotebookAttachment {
  kind: 'file' | 'image' | 'audio' | 'document' | 'selection';
  fileId?: string;
  uri?: string;
  mimeType?: string;
  label?: string;
  metadata?: Record<string, JsonValue>;
}
```

From `src/services/assistant-runtime/createAssistantRuntime.ts`:
```typescript
export interface AssistantRuntimeExecutionOptions {
  toolsCallback?: ToolCallback;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define unified tool, media, and delivery contracts</name>
  <files>src/services/assistant-runtime/types.ts, src/services/assistant-runtime/toolMediaContracts.ts, test/services/assistantToolRuntimeContracts.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/types.ts
    - src/services/assistant-runtime/createAssistantRuntime.ts
    - services/ai/providerTypes.ts
    - services/toolCallAdapters.ts
    - .planning/phases/03-tools-and-multimodal-delivery/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The runtime has explicit tool execution contracts instead of caller-owned callback assumptions.
    - Test 2: Text, image, audio, and document inputs map into one normalized media contract surface.
    - Test 3: Delivery policy outputs are typed as transport-neutral units rather than channel-specific payloads.
  </behavior>
  <action>Introduce a dedicated Phase 3 contract module and extend runtime exports so downstream work can implement unified tool execution, multimodal normalization, and delivery policy without changing schemas mid-phase.</action>
  <acceptance_criteria>
    - `rg -n "Tool|Media|Delivery" src/services/assistant-runtime/toolMediaContracts.ts src/services/assistant-runtime/types.ts`
    - `bun run vitest run test/services/assistantToolRuntimeContracts.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantToolRuntimeContracts.test.ts</automated>
  </verify>
  <done>Phase 3 has one shared schema surface for tool execution, media normalization, and delivery units.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export and lock delivery-policy contracts with regression tests</name>
  <files>src/services/assistant-runtime/index.ts, test/services/assistantDeliveryPolicyContracts.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/index.ts
    - src/services/assistant-runtime/toolMediaContracts.ts
    - .planning/phases/03-tools-and-multimodal-delivery/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: The runtime barrel exports the Phase 3 contract surface for downstream plans.
    - Test 2: Delivery-policy contracts remain transport-neutral and future-channel-safe.
    - Test 3: Contract tests fail if delivery-unit or tool/media status types drift.
  </behavior>
  <action>Expose the new contract surface through the runtime barrel and add focused tests so later implementation plans can depend on one stable import path.</action>
  <acceptance_criteria>
    - `rg -n "toolMediaContracts|Delivery" src/services/assistant-runtime/index.ts`
    - `bun run vitest run test/services/assistantDeliveryPolicyContracts.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantDeliveryPolicyContracts.test.ts</automated>
  </verify>
  <done>Downstream plans can import one stable contract surface from the runtime barrel.</done>
</task>

</tasks>

<verification>
Run the contract tests and confirm Phase 3 implementation plans can target one explicit tool/media/delivery schema surface.
</verification>

<success_criteria>
Phase 3 implementation no longer needs to infer tool, media, or delivery behavior from caller-specific code paths because the shared runtime contracts define those seams explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tools-and-multimodal-delivery/03-01-SUMMARY.md`
</output>
