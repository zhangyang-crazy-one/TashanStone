---
phase: 03-tools-and-multimodal-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/services/assistant-runtime/toolExecutor.ts
  - src/services/assistant-runtime/createAssistantRuntime.ts
  - src/app/hooks/useAIWorkflow.ts
  - services/ai/toolDefinitions.ts
  - test/services/assistantToolExecution.test.ts
  - test/services/assistantToolStatusFlow.test.ts
autonomous: true
requirements:
  - TOOL-01
  - TOOL-04
must_haves:
  truths:
    - "Tool execution is owned by the shared runtime seam rather than by app-specific callback logic inside useAIWorkflow."
    - "Existing MCP tools and built-in notebook capabilities flow through one execution path."
    - "Tool status remains visible through explicit runtime events and UI-consumable payloads."
  artifacts:
    - path: "src/services/assistant-runtime/toolExecutor.ts"
      provides: "Unified runtime-owned tool execution substrate"
    - path: "test/services/assistantToolStatusFlow.test.ts"
      provides: "Regression coverage for visible running/success/error tool states"
  key_links:
    - from: "src/services/assistant-runtime/toolExecutor.ts"
      to: "services/ai/toolDefinitions.ts"
      via: "The runtime executor must reuse existing provider tool schemas and built-in capabilities"
      pattern: "create_file|search_knowledge_base|tool"
    - from: "src/services/assistant-runtime/createAssistantRuntime.ts"
      to: "test/services/assistantToolStatusFlow.test.ts"
      via: "Runtime lifecycle and tool-status emissions remain the visible contract for callers"
      pattern: "tool-status|running|success|error"
---

<objective>
Implement the shared tool execution layer so TashanStone capabilities and MCP tools run through one runtime-owned execution path with visible status updates, and move notebook tool orchestration out of the app hook.

Purpose: satisfy `TOOL-01` and the execution-status side of `TOOL-04`.
Output: a tool executor integrated with runtime events and tests proving unified tool execution plus visible status flow.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-tools-and-multimodal-delivery/03-RESEARCH.md
@.planning/phases/03-tools-and-multimodal-delivery/03-01-PLAN.md
@src/services/assistant-runtime/createAssistantRuntime.ts
@services/ai/toolDefinitions.ts
@services/toolCallAdapters.ts
@src/app/hooks/useAIWorkflow.ts

<interfaces>
From `src/services/assistant-runtime/createAssistantRuntime.ts`:
```typescript
toolEventCallback: toolCall => {
  emit({ type: 'tool-status', ... });
}
```

From `src/app/hooks/useAIWorkflow.ts`:
```typescript
if (toolName === 'create_file') { ... }
if (toolName === 'update_file') { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a unified runtime-owned tool executor</name>
  <files>src/services/assistant-runtime/toolExecutor.ts, services/ai/toolDefinitions.ts, test/services/assistantToolExecution.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/toolMediaContracts.ts
    - services/ai/toolDefinitions.ts
    - services/ai/providerTypes.ts
    - src/app/hooks/useAIWorkflow.ts
    - .planning/phases/03-tools-and-multimodal-delivery/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Built-in notebook tools and MCP-backed tools can run through one executor surface.
    - Test 2: Provider-facing tool definitions remain compatible with the unified executor.
    - Test 3: Callers no longer need bespoke tool orchestration logic in `useAIWorkflow.ts` to invoke notebook capabilities.
  </behavior>
  <action>Create a runtime-side tool executor that wraps existing built-in capabilities and MCP tool access behind one execution seam, while reusing the existing provider tool definition layer.</action>
  <acceptance_criteria>
    - `rg -n "execute|registry|tool" src/services/assistant-runtime/toolExecutor.ts services/ai/toolDefinitions.ts`
    - `bun run vitest run test/services/assistantToolExecution.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantToolExecution.test.ts</automated>
  </verify>
  <done>The shared runtime owns tool execution instead of delegating orchestration to surface-specific callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move in-app notebook tool orchestration onto the runtime-owned executor seam</name>
  <files>src/services/assistant-runtime/createAssistantRuntime.ts, src/app/hooks/useAIWorkflow.ts, test/services/assistantToolStatusFlow.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/createAssistantRuntime.ts
    - src/app/hooks/useAIWorkflow.ts
    - test/services/inAppAssistantRuntimeAdapter.test.ts
  </read_first>
  <behavior>
    - Test 1: Running, success, and error tool states propagate through runtime events in one consistent format.
    - Test 2: `useAIWorkflow.ts` stops owning built-in notebook tool branching and delegates to the runtime-owned executor seam.
    - Test 3: Existing in-app runtime consumers remain compatible with the new tool-execution path.
  </behavior>
  <action>Integrate the unified tool executor into runtime execution and migrate the in-app hook off its built-in tool branching so provider flows and app callers both rely on the same runtime-owned executor.</action>
  <acceptance_criteria>
    - `rg -n "toolExecutor|tool-status" src/services/assistant-runtime/createAssistantRuntime.ts src/app/hooks/useAIWorkflow.ts`
    - `rg -n "create_file|update_file|delete_file|read_file|search_files" src/app/hooks/useAIWorkflow.ts`
    - `bun run vitest run test/services/assistantToolStatusFlow.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantToolStatusFlow.test.ts</automated>
  </verify>
  <done>Tool execution is visible, shared, and runtime-owned across provider flows.</done>
</task>

</tasks>

<verification>
Run the tool execution and tool-status tests, then confirm notebook capabilities and MCP tools now share one runtime execution path and that the app hook no longer owns notebook tool branching.
</verification>

<success_criteria>
Phase 3 no longer relies on caller-owned tool orchestration, and tool execution status is emitted in a reusable runtime form.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tools-and-multimodal-delivery/03-02-SUMMARY.md`
</output>
