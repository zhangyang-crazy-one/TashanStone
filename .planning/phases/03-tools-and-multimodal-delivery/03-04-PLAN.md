---
phase: 03-tools-and-multimodal-delivery
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
  - 03-02
files_modified:
  - src/services/assistant-runtime/deliveryPolicy.ts
  - src/services/assistant-runtime/providerExecution.ts
  - src/app/hooks/useAIWorkflow.ts
  - components/ToolCallCard.tsx
  - components/StreamToolCard.tsx
  - test/services/inAppAssistantDeliveryPolicy.test.ts
  - test/services/inAppAssistantToolMediaStatus.test.ts
autonomous: true
requirements:
  - TOOL-03
  - TOOL-04
must_haves:
  truths:
    - "Outbound assistant responses can be chunked and described through one transport-neutral delivery policy layer with explicit per-channel policy selection."
    - "The existing in-app assistant surface proves delivery and tool/media status without introducing channel-specific code."
    - "Long responses, tool execution, and media handling are visible to users instead of silently collapsing into one opaque text blob."
  artifacts:
    - path: "src/services/assistant-runtime/deliveryPolicy.ts"
      provides: "Transport-neutral outbound chunking plus configurable per-channel policy selection"
    - path: "test/services/inAppAssistantToolMediaStatus.test.ts"
      provides: "Regression coverage for visible in-app delivery/tool/media status behavior"
  key_links:
    - from: "src/services/assistant-runtime/deliveryPolicy.ts"
      to: "src/services/assistant-runtime/providerExecution.ts"
      via: "Chunking and delivery behavior should consume runtime outputs without becoming provider-specific"
      pattern: "chunk|delivery|policy|channel|profile|stream"
    - from: "src/app/hooks/useAIWorkflow.ts"
      to: "components/StreamToolCard.tsx"
      via: "The app proof should reuse the existing status card surfaces rather than inventing a new one"
      pattern: "toolCalls|status|stream"
---

<objective>
Implement the outbound delivery-policy layer and prove it through the in-app assistant so long responses and tool/media processing show visible, reusable status behavior while delivery rules stay configurable per channel.

Purpose: satisfy `TOOL-03` and complete `TOOL-04`.
Output: a transport-neutral, channel-configurable delivery policy plus an in-app proof that chunking and tool/media status are surfaced through the existing assistant UI.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tools-and-multimodal-delivery/03-RESEARCH.md
@.planning/phases/03-tools-and-multimodal-delivery/03-02-PLAN.md
@.planning/phases/03-tools-and-multimodal-delivery/03-03-PLAN.md
@src/services/assistant-runtime/providerExecution.ts
@src/app/hooks/useAIWorkflow.ts
@components/ToolCallCard.tsx
@components/StreamToolCard.tsx

<interfaces>
From `src/services/assistant-runtime/providerExecution.ts`:
```typescript
onStreamDelta?: (delta: string, accumulatedText: string) => void;
```

From `components/StreamToolCard.tsx`:
```typescript
status: 'executing' | 'success' | 'error';
result?: string;
progress?: number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement a transport-neutral delivery policy and chunking layer</name>
  <files>src/services/assistant-runtime/deliveryPolicy.ts, src/services/assistant-runtime/providerExecution.ts, test/services/inAppAssistantDeliveryPolicy.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/toolMediaContracts.ts
    - src/services/assistant-runtime/providerExecution.ts
    - services/ai/aiStreamingPipeline.ts
    - .planning/phases/03-tools-and-multimodal-delivery/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Long assistant outputs can be chunked into transport-neutral delivery units according to a selected delivery policy profile.
    - Test 2: Different channel policy configs can be selected (for example in-app, WhatsApp, QQ placeholder profiles) without branching inside callers.
    - Test 3: Delivery policy remains decoupled from WhatsApp/QQ formatter specifics even when profiles differ.
    - Test 4: Streaming and non-streaming runtime outputs both map cleanly into delivery-policy results.
  </behavior>
  <action>Add a dedicated delivery-policy module with explicit policy config/profile selection and integrate it with provider execution so runtime outputs can be chunked and described generically before later channel phases consume them.</action>
  <acceptance_criteria>
    - `rg -n "chunk|delivery|policy|channel|profile|maxChars|splitPreference" src/services/assistant-runtime/deliveryPolicy.ts src/services/assistant-runtime/providerExecution.ts`
    - `bun run vitest run test/services/inAppAssistantDeliveryPolicy.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantDeliveryPolicy.test.ts</automated>
  </verify>
  <done>The runtime has one reusable outbound delivery policy instead of provider- or channel-specific chunking logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove visible delivery and tool/media status in the in-app assistant</name>
  <files>src/app/hooks/useAIWorkflow.ts, components/ToolCallCard.tsx, components/StreamToolCard.tsx, test/services/inAppAssistantToolMediaStatus.test.ts</files>
  <read_first>
    - src/app/hooks/useAIWorkflow.ts
    - components/ToolCallCard.tsx
    - components/StreamToolCard.tsx
    - test/services/inAppAssistantRuntimeAdapter.test.ts
    - .planning/phases/03-tools-and-multimodal-delivery/03-03-PLAN.md
  </read_first>
  <behavior>
    - Test 1: In-app assistant consumers can surface chunking, tool execution, and media-processing states through the existing status cards.
    - Test 2: Errors and progress remain visible rather than collapsing into plain text.
    - Test 3: The proof shows that different delivery policy profiles can be selected without expanding into channel-specific UI code.
    - Test 4: The proof stays narrow and transport-neutral, leaving full parity work for Phase 4.
  </behavior>
  <action>Wire the delivery and status outputs into the existing in-app runtime hook and status card components so Phase 3 proves configurable policy selection and visible behavior without expanding into a broader UI redesign.</action>
  <acceptance_criteria>
    - `rg -n "toolCalls|status|progress|stream" src/app/hooks/useAIWorkflow.ts components/ToolCallCard.tsx components/StreamToolCard.tsx`
    - `rg -n "policy|channel|profile" src/services/assistant-runtime/deliveryPolicy.ts src/app/hooks/useAIWorkflow.ts`
    - `bun run vitest run test/services/inAppAssistantToolMediaStatus.test.ts`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantToolMediaStatus.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>The app proves transport-neutral delivery and visible tool/media status through the existing assistant surfaces.</done>
</task>

</tasks>

<verification>
Run the delivery-policy and in-app status tests, then confirm Phase 3 now exposes transport-neutral but channel-configurable chunking and visible tool/media handling without channel-specific code.
</verification>

<success_criteria>
Phase 3 ends with one shared tool/media execution substrate, one multimodal normalization path, and one channel-configurable, transport-neutral delivery policy proven through the in-app assistant.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tools-and-multimodal-delivery/03-04-SUMMARY.md`
</output>
