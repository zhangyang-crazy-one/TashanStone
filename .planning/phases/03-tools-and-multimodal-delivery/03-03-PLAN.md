---
phase: 03-tools-and-multimodal-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/services/assistant-runtime/multimodalNormalizer.ts
  - src/services/assistant-runtime/providerInputAdapter.ts
  - src/services/assistant-runtime/providerExecution.ts
  - src/services/assistant-runtime/createAssistantRuntime.ts
  - src/app/hooks/useAIWorkflow.ts
  - services/ocrService.ts
  - src/types/electronAPI.ts
  - test/services/assistantMultimodalNormalization.test.ts
  - test/services/assistantMediaStatusFlow.test.ts
autonomous: true
requirements:
  - TOOL-02
  - TOOL-04
must_haves:
  truths:
    - "Text, image, audio, and document inputs normalize into one provider-neutral assistant input model before provider execution."
    - "Existing OCR and transcription capabilities are reused through normalization adapters rather than replaced."
    - "Media normalization and extraction lifecycle events are emitted through the runtime event bridge so callers can surface running/success/error states."
  artifacts:
    - path: "src/services/assistant-runtime/multimodalNormalizer.ts"
      provides: "Canonical normalization path for text/image/audio/document assistant inputs"
    - path: "src/services/assistant-runtime/createAssistantRuntime.ts"
      provides: "Runtime event bridge for media-status and media-processing failure visibility"
    - path: "test/services/assistantMediaStatusFlow.test.ts"
      provides: "Regression coverage for visible media-processing success and failure states"
  key_links:
    - from: "src/services/assistant-runtime/multimodalNormalizer.ts"
      to: "services/ocrService.ts"
      via: "Image/document inputs must reuse existing OCR capabilities through a normalized seam"
      pattern: "recognize|image|document"
    - from: "src/services/assistant-runtime/providerInputAdapter.ts"
      to: "src/types/electronAPI.ts"
      via: "Audio and OCR entrypoints should be consumed through existing Electron bridges when normalization needs them"
      pattern: "ocr|whisper|sherpa"
    - from: "src/services/assistant-runtime/createAssistantRuntime.ts"
      to: "test/services/assistantMediaStatusFlow.test.ts"
      via: "Media preprocessing must emit first-class runtime events that callers can render"
      pattern: "media-status|running|success|error"
---

<objective>
Implement multimodal normalization so text, images, audio, and documents enter the assistant runtime through one typed, provider-neutral preparation path and are actually wired into runtime/provider execution with visible status/error signaling.

Purpose: satisfy `TOOL-02` and the media-handling visibility side of `TOOL-04`.
Output: normalization/adaptation modules plus tests for normalized media payloads and visible runtime-level media-processing status.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tools-and-multimodal-delivery/03-RESEARCH.md
@.planning/phases/03-tools-and-multimodal-delivery/03-01-PLAN.md
@src/services/assistant-runtime/types.ts
@src/services/assistant-runtime/providerExecution.ts
@src/services/assistant-runtime/createAssistantRuntime.ts
@services/ocrService.ts
@src/types/electronAPI.ts
@src/app/hooks/useAIWorkflow.ts

<interfaces>
From `src/services/assistant-runtime/types.ts`:
```typescript
export interface AssistantNotebookAttachment {
  kind: 'file' | 'image' | 'audio' | 'document' | 'selection';
}
```

From `services/ocrService.ts`:
```typescript
async recognize(imageSource: HTMLImageElement | HTMLCanvasElement | string): Promise<OcrResult>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize multimodal assistant inputs before provider execution</name>
  <files>src/services/assistant-runtime/multimodalNormalizer.ts, src/services/assistant-runtime/providerInputAdapter.ts, test/services/assistantMultimodalNormalization.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/toolMediaContracts.ts
    - src/services/assistant-runtime/types.ts
    - services/ocrService.ts
    - src/types/electronAPI.ts
    - .planning/phases/03-tools-and-multimodal-delivery/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Text, image, audio, and document inputs normalize into one shared assistant media model.
    - Test 2: Provider-facing preparation remains provider-neutral and does not leak OCR/transcription implementation details into callers.
    - Test 3: Normalization preserves provenance metadata needed for later delivery/status handling.
  </behavior>
  <action>Create dedicated normalization and provider-input adaptation modules that convert raw text/image/audio/document inputs into the shared Phase 3 media model.</action>
  <acceptance_criteria>
    - `rg -n "normalize|image|audio|document|text" src/services/assistant-runtime/multimodalNormalizer.ts src/services/assistant-runtime/providerInputAdapter.ts`
    - `bun run vitest run test/services/assistantMultimodalNormalization.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantMultimodalNormalization.test.ts</automated>
  </verify>
  <done>The runtime can accept normalized multimodal inputs through one provider-neutral preparation path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire normalized multimodal inputs into runtime/provider execution and app callers</name>
  <files>src/services/assistant-runtime/providerExecution.ts, src/services/assistant-runtime/createAssistantRuntime.ts, src/app/hooks/useAIWorkflow.ts, services/ocrService.ts, src/types/electronAPI.ts, test/services/assistantMediaStatusFlow.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/providerExecution.ts
    - src/app/hooks/useAIWorkflow.ts
    - services/ocrService.ts
    - src/types/electronAPI.ts
    - src/services/assistant-runtime/multimodalNormalizer.ts
    - src/services/assistant-runtime/createAssistantRuntime.ts
  </read_first>
  <behavior>
    - Test 1: Provider execution can consume normalized text/image/audio/document inputs instead of remaining prompt-only.
    - Test 2: `createAssistantRuntime.ts` emits first-class `media-status` lifecycle events so callers can observe running/success/error states during OCR, transcription, or extraction work.
    - Test 3: The in-app caller stops declaring `multimodalInput: false` and passes normalized attachments into runtime execution where supported.
    - Test 4: Media normalization and extraction failures return explicit runtime-visible status/error payloads instead of silently dropping unsupported inputs.
  </behavior>
  <action>Refine the Electron-facing media services and their types, add runtime event emission for the media-processing lifecycle, then integrate normalized multimodal inputs into provider execution and the in-app runtime caller so Phase 3 actually satisfies `TOOL-02` and the media-visibility portion of `TOOL-04` end to end.</action>
  <acceptance_criteria>
    - `rg -n "media-status|attachments|multimodalInput|audio|image|document" src/services/assistant-runtime/providerExecution.ts src/services/assistant-runtime/createAssistantRuntime.ts src/app/hooks/useAIWorkflow.ts`
    - `rg -n "status|error|media|ocr|audio" services/ocrService.ts src/types/electronAPI.ts`
    - `bun run vitest run test/services/assistantMediaStatusFlow.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantMediaStatusFlow.test.ts</automated>
  </verify>
  <done>Media-processing behavior is explicit, typed, and surfaced through runtime events that callers can render.</done>
</task>

</tasks>

<verification>
Run the multimodal normalization and media-status tests, then confirm Phase 3 can normalize text/image/audio/document inputs, feed them into runtime execution, and emit caller-visible media lifecycle events without silent failure.
</verification>

<success_criteria>
Phase 3 no longer treats multimodal support as a vague provider capability; the repo has one explicit normalization path, one real execution integration path, and one runtime-visible media-status contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-tools-and-multimodal-delivery/03-03-SUMMARY.md`
</output>
