---
phase: 04-in-app-assistant-parity
plan: 06
type: execute
wave: 2
depends_on:
  - 04-05
files_modified:
  - App.tsx
  - components/App/AppShell.tsx
  - components/App/AppWorkspace.tsx
  - components/ChatPanel.tsx
  - src/app/hooks/useAIWorkflow.ts
  - src/app/hooks/useAppWorkspaceState.ts
  - test/components/AppChatContextThreading.test.tsx
  - test/services/inAppAssistantWorkspaceContextControls.test.ts
autonomous: true
requirements:
  - APP-01
  - APP-02
gap_closure: true
must_haves:
  truths:
    - "Focused notebook grounding choices exist as explicit app/chat state instead of only implicit hook defaults."
    - "The shared runtime honors `Focused note only` versus `Open panes` and optional selected-text injection when assembling assistant requests."
    - "A targeted test proves `contextScope` and `includeSelectedText` are threaded from the app shell into `ChatPanel` before the follow-on UI plan binds visible controls to them."
  artifacts:
    - path: "App.tsx"
      provides: "App-owned assistant context-control state that is threaded through the shell instead of remaining hidden in hook defaults"
    - path: "components/ChatPanel.tsx"
      provides: "Chat-surface contract that receives explicit context-scope and selected-text inclusion state"
    - path: "src/app/hooks/useAIWorkflow.ts"
      provides: "Shared runtime request assembly that consumes explicit workspace-context choices"
    - path: "test/components/AppChatContextThreading.test.tsx"
      provides: "Component/integration proof that app-shell state reaches ChatPanel"
    - path: "test/services/inAppAssistantWorkspaceContextControls.test.ts"
      provides: "Regression coverage for explicit workspace-context choices flowing into runtime requests"
  key_links:
    - from: "App.tsx"
      to: "components/ChatPanel.tsx"
      via: "typed `contextScope` and `includeSelectedText` state must reach the chat surface so follow-on UI work can bind to it without reopening the shell"
      pattern: "contextScope|includeSelectedText|workspaceContext"
    - from: "components/ChatPanel.tsx"
      to: "src/app/hooks/useAIWorkflow.ts"
      via: "chat-surface context choices must drive request assembly for focused-note versus open-pane grounding"
      pattern: "contextScope|includeSelectedText|selectedFileIds|selectedText"
---

<objective>
Close only the runtime/app-shell half of the workspace-context UAT gap by threading explicit notebook-context choices through the app shell and into the shared runtime.

Purpose: satisfy the runtime-backed grounding behavior in `APP-01` and create the inspectable chat-surface contract required for `APP-02` without mixing in the visible UI work or composer rewrite.
Output: explicit assistant context-control state threaded from `App.tsx` to `ChatPanel` and into runtime request assembly, plus verification that proves the state reaches the chat surface.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-UAT.md
@.planning/phases/04-in-app-assistant-parity/04-02-SUMMARY.md
@App.tsx
@components/App/AppShell.tsx
@components/App/AppWorkspace.tsx
@components/ChatPanel.tsx
@src/app/hooks/useAppWorkspaceState.ts
@src/app/hooks/useAIWorkflow.ts
@test/services/inAppAssistantWorkspaceContext.test.ts
@test/services/inAppAssistantWorkspaceContextControls.test.ts

<interfaces>
From `src/app/hooks/useAppWorkspaceState.ts`:
```typescript
workspaceContext: {
  activeFileId?: string;
  selectedFileIds: string[];
  selectedText?: string;
};
```

From `src/app/hooks/useAIWorkflow.ts`:
```typescript
workspaceContext?: {
  activeFileId?: string;
  selectedFileIds?: string[];
  selectedText?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread explicit workspace-context choices through the app shell into runtime requests</name>
  <files>App.tsx, components/App/AppShell.tsx, components/App/AppWorkspace.tsx, components/ChatPanel.tsx, src/app/hooks/useAppWorkspaceState.ts, src/app/hooks/useAIWorkflow.ts, test/services/inAppAssistantWorkspaceContextControls.test.ts</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - App.tsx
    - components/App/AppShell.tsx
    - components/App/AppWorkspace.tsx
    - components/ChatPanel.tsx
    - src/app/hooks/useAppWorkspaceState.ts
    - src/app/hooks/useAIWorkflow.ts
    - test/services/inAppAssistantWorkspaceContext.test.ts
  </read_first>
  <behavior>
    - Test 1: The runtime request accepts explicit `contextScope` and `includeSelectedText` values instead of always deriving notebook grounding from hidden defaults.
    - Test 2: `Focused note only` resolves to the active note path and attachments, while `Open panes` continues to use the open-pane file set.
    - Test 3: Selected text is injected only when `includeSelectedText` is true, so later UI controls can safely expose the toggle.
  </behavior>
  <action>Split the runtime plumbing from the visible UI so this task does only the cross-shell threading work. Extend the app/chat contract with typed `contextScope` and `includeSelectedText` state plus setter callbacks in `App.tsx`, `components/App/AppShell.tsx`, `components/App/AppWorkspace.tsx`, and `components/ChatPanel.tsx`, then update `useAIWorkflow.ts` so request assembly follows explicit `Focused note only`, `Open panes`, and selected-text inclusion choices. Do not build the final `WorkspaceContextPanel`, header affordance, or multiline composer here; this task exists specifically to isolate runtime propagation from later UI work.</action>
  <acceptance_criteria>
    - `rg -n "contextScope|includeSelectedText|workspaceContext" App.tsx src/app/hooks/useAppWorkspaceState.ts src/app/hooks/useAIWorkflow.ts`
    - `bun run vitest run test/services/inAppAssistantWorkspaceContext.test.ts test/services/inAppAssistantWorkspaceContextControls.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantWorkspaceContext.test.ts test/services/inAppAssistantWorkspaceContextControls.test.ts</automated>
  </verify>
  <done>The shared runtime now accepts explicit workspace-context choices from app state, and the next UI-focused plan can bind visible chat controls to that contract without reopening the app shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a component/integration proof that app-shell state reaches ChatPanel</name>
  <files>test/components/AppChatContextThreading.test.tsx</files>
  <read_first>
    - App.tsx
    - components/App/AppShell.tsx
    - components/ChatPanel.tsx
    - src/app/hooks/useAIWorkflow.ts
    - test/services/inAppAssistantWorkspaceContextControls.test.ts
  </read_first>
  <behavior>
    - Test 1: Rendering the app-shell path proves `ChatPanel` receives `contextScope` and `includeSelectedText` props from the top-level app state.
    - Test 2: The test fails if those values stop being threaded through `App.tsx -> AppShell.tsx -> AppWorkspace.tsx -> ChatPanel.tsx`.
    - Test 3: The test does not depend on the follow-on visible workspace-context panel, so it remains a stable contract guard for future UI work.
  </behavior>
  <action>Create a focused component or lightweight integration test that exercises the app-shell route into `ChatPanel` and asserts the threaded `contextScope` and `includeSelectedText` values arrive intact. Prefer mocking the final `ChatPanel` boundary if that keeps the test surgical; the goal is to prove the checker-flagged wiring path rather than re-test runtime assembly.</action>
  <acceptance_criteria>
    - `rg -n "contextScope|includeSelectedText|ChatPanel" test/components/AppChatContextThreading.test.tsx`
    - `bun run vitest run test/components/AppChatContextThreading.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/AppChatContextThreading.test.tsx</automated>
  </verify>
  <done>There is now an automated proof that the app-shell runtime contract actually reaches `ChatPanel`, which closes the checker gap on 04-06 verification scope.</done>
</task>

</tasks>

<verification>
Run the runtime-context service tests and the app-shell threading component test. Confirm the runtime honors explicit scope choices and that `ChatPanel` receives the state the follow-on UI plans will expose.
</verification>

<success_criteria>
The runtime contract for user-selectable workspace context exists independently of the UI surface, and automated coverage now proves the state reaches `ChatPanel` instead of stopping at service-only verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md`
</output>
