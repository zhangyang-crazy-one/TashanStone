---
phase: 04-in-app-assistant-parity
plan: 06
type: execute
wave: 2
depends_on:
  - 04-05
files_modified:
  - App.tsx
  - components/App/AppShell.tsx
  - components/App/AppWorkspace.tsx
  - components/ChatPanel.tsx
  - components/ChatPanel/ChatInput.tsx
  - src/app/hooks/useAIWorkflow.ts
  - src/app/hooks/useAppWorkspaceState.ts
  - utils/translations.ts
  - test/components/ChatInput.test.tsx
  - test/components/ChatPanelParity.test.tsx
  - test/services/inAppAssistantWorkspaceContextControls.test.ts
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
gap_closure: true
must_haves:
  truths:
    - "Focused notebook context no longer has to follow hidden open-pane defaults once chat-surface controls choose a scope and selected-text policy."
    - "The shared runtime can honor explicit `Focused note only` versus `Open panes` choices without new app-shell rewiring."
    - "Long prompts wrap and grow in the composer instead of being truncated on one line."
  artifacts:
    - path: "src/app/hooks/useAIWorkflow.ts"
      provides: "Shared runtime request assembly that accepts explicit workspace-context choices instead of only hidden defaults"
    - path: "App.tsx"
      provides: "App-shell threading for context scope and selected-text policy so later UI work does not need to reopen runtime plumbing"
    - path: "components/ChatPanel/ChatInput.tsx"
      provides: "Multiline auto-growing composer with explicit submit/newline behavior"
    - path: "test/services/inAppAssistantWorkspaceContextControls.test.ts"
      provides: "Regression coverage for explicit workspace-context choices flowing into runtime requests"
  key_links:
    - from: "src/app/hooks/useAppWorkspaceState.ts"
      to: "src/app/hooks/useAIWorkflow.ts"
      via: "explicit context scope and selected-text inclusion must drive runtime request attachments"
      pattern: "selectedFileIds|selectedText|workspaceContext"
    - from: "App.tsx"
      to: "components/ChatPanel.tsx"
      via: "typed context-control state must be threaded into the chat surface so follow-on UI work can bind to it without reopening the app shell"
      pattern: "contextScope|includeSelectedText|workspaceContext"
---

<objective>
Close the runtime-plumbing half of the workspace-context UAT gap and the composer UAT gap by threading explicit notebook-context choices through the shared runtime, then replacing the truncating single-line composer.

Purpose: satisfy the runtime-backed grounding behavior in `APP-01`, lay the contract that the follow-on UI plan will expose for `APP-02`, and protect notebook workflow usability required by `APP-03`.
Output: app-shell/runtime context plumbing that accepts explicit scope choices, plus a multiline auto-growing composer wired into the shared runtime path.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-UAT.md
@.planning/phases/04-in-app-assistant-parity/04-02-SUMMARY.md
@.planning/phases/04-in-app-assistant-parity/04-04-SUMMARY.md
@App.tsx
@components/App/AppShell.tsx
@components/App/AppWorkspace.tsx
@components/ChatPanel.tsx
@components/ChatPanel/ChatInput.tsx
@src/app/hooks/useAppWorkspaceState.ts
@src/app/hooks/useAIWorkflow.ts
@test/services/inAppAssistantWorkspaceContext.test.ts
@test/components/ChatPanelParity.test.tsx

<interfaces>
From `src/app/hooks/useAppWorkspaceState.ts`:
```typescript
workspaceContext: {
  activeFileId?: string;
  selectedFileIds: string[];
  selectedText?: string;
};
```

From `components/ChatPanel/ChatInput.tsx`:
```typescript
interface ChatInputProps {
  input: string;
  onInputChange: (value: string) => void;
  onSubmit: (event: React.FormEvent) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread explicit workspace-context choices through the app shell into runtime requests</name>
  <files>App.tsx, components/App/AppShell.tsx, components/App/AppWorkspace.tsx, components/ChatPanel.tsx, src/app/hooks/useAppWorkspaceState.ts, src/app/hooks/useAIWorkflow.ts, test/services/inAppAssistantWorkspaceContextControls.test.ts</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - App.tsx
    - components/App/AppShell.tsx
    - components/App/AppWorkspace.tsx
    - components/ChatPanel.tsx
    - src/app/hooks/useAppWorkspaceState.ts
    - src/app/hooks/useAIWorkflow.ts
    - test/services/inAppAssistantWorkspaceContext.test.ts
  </read_first>
  <behavior>
    - Test 1: The runtime request accepts explicit `contextScope` and `includeSelectedText` values instead of always deriving notebook grounding from hidden defaults.
    - Test 2: `Focused note only` resolves to the active note path and attachments, while `Open panes` continues to use the open-pane file set.
    - Test 3: Selected text is injected only when `includeSelectedText` is true, so later UI controls can safely expose the toggle.
  </behavior>
  <action>Split the runtime plumbing from the eventual `WorkspaceContextPanel` UI so this plan does only the cross-shell threading work. Introduce typed chat-surface context control state in `App.tsx`, thread it through `components/App/AppShell.tsx`, `components/App/AppWorkspace.tsx`, and `components/ChatPanel.tsx`, and update `useAIWorkflow.ts` so request assembly follows explicit `Focused note only`, `Open panes`, and `includeSelectedText` choices. Do not build the final panel or header affordance here; this task exists to address the checker concern by isolating app-shell/runtime propagation from the later UI task.</action>
  <acceptance_criteria>
    - `rg -n "contextScope|includeSelectedText|workspaceContext" App.tsx src/app/hooks/useAppWorkspaceState.ts src/app/hooks/useAIWorkflow.ts`
    - `bun run vitest run test/services/inAppAssistantWorkspaceContext.test.ts test/services/inAppAssistantWorkspaceContextControls.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantWorkspaceContext.test.ts test/services/inAppAssistantWorkspaceContextControls.test.ts</automated>
  </verify>
  <done>The shared runtime now accepts explicit workspace-context choices from app state, and the next UI-focused plan can bind chat controls to that contract without reopening the app shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the single-line composer with a multiline auto-growing chat input</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatInput.tsx, utils/translations.ts, test/components/ChatInput.test.tsx, test/components/ChatPanelParity.test.tsx</files>
  <read_first>
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatInput.tsx
    - utils/translations.ts
    - test/components/ChatPanelParity.test.tsx
  </read_first>
  <behavior>
    - Test 1: Long prompts wrap across lines and the composer grows vertically instead of clipping text.
    - Test 2: `Enter` submits the prompt and `Shift+Enter` inserts a newline.
    - Test 3: Voice-input append behavior still lands in the multiline composer without regressing send behavior.
  </behavior>
  <action>Replace the current single-line `<input>` with a multiline auto-growing `<textarea>` in `ChatInput.tsx`. Keep the current send button and voice-input affordance, but implement explicit keyboard behavior: `Enter` submits and `Shift+Enter` inserts a newline. Preserve existing injected-message submit flow from `ChatPanel.tsx` and update tests so multiline prompts are visible and submittable.</action>
  <acceptance_criteria>
    - `rg -n "textarea|Shift\\+Enter|Enter" components/ChatPanel/ChatInput.tsx test/components/ChatInput.test.tsx`
    - `bun run vitest run test/components/ChatInput.test.tsx test/components/ChatPanelParity.test.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/ChatInput.test.tsx test/components/ChatPanelParity.test.tsx && bun run tsc --noEmit</automated>
  </verify>
  <done>The in-app assistant composer is multiline, auto-growing, and keyboard-correct, so long prompts remain visible and usable inside the parity chat surface.</done>
</task>

</tasks>

<verification>
Run the runtime-context and multiline-composer tests. Confirm the shared runtime now honors explicit scope choices and the composer no longer truncates long prompts.
</verification>

<success_criteria>
The runtime contract for user-selectable workspace context exists independently of the UI surface, and longer prompts no longer truncate in a single-line composer.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md`
</output>
