---
phase: 04-in-app-assistant-parity
plan: 06
type: execute
wave: 2
depends_on:
  - 04-05
files_modified:
  - App.tsx
  - components/App/AppShell.tsx
  - components/App/AppWorkspace.tsx
  - components/ChatPanel.tsx
  - components/ChatPanel/ChatHeader.tsx
  - components/ChatPanel/ChatInput.tsx
  - components/ChatPanel/WorkspaceContextPanel.tsx
  - src/app/hooks/useAIWorkflow.ts
  - src/app/hooks/useAppWorkspaceState.ts
  - utils/translations.ts
  - test/components/ChatInput.test.tsx
  - test/components/WorkspaceContextPanel.test.tsx
  - test/services/inAppAssistantWorkspaceContextControls.test.ts
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
gap_closure: true
must_haves:
  truths:
    - "Users can see exactly which note scope and selected text will be injected into the assistant before they send a prompt."
    - "Users can adjust notebook context scope from the chat surface instead of relying on invisible hook-level defaults."
    - "Long prompts wrap and grow in the composer instead of being truncated on one line."
  artifacts:
    - path: "components/ChatPanel/WorkspaceContextPanel.tsx"
      provides: "Visible chat-surface control for inspecting and adjusting injected notebook context"
    - path: "components/ChatPanel/ChatInput.tsx"
      provides: "Multiline auto-growing composer with explicit submit/newline behavior"
    - path: "test/services/inAppAssistantWorkspaceContextControls.test.ts"
      provides: "Regression coverage for user-adjustable workspace context flowing into runtime requests"
  key_links:
    - from: "src/app/hooks/useAppWorkspaceState.ts"
      to: "src/app/hooks/useAIWorkflow.ts"
      via: "user-selected context scope and selected-text inclusion must drive runtime request attachments"
      pattern: "selectedFileIds|selectedText|workspaceContext"
    - from: "components/ChatPanel/WorkspaceContextPanel.tsx"
      to: "components/ChatPanel.tsx"
      via: "the panel must reflect live workspace facts and update the context controls used for submit"
      pattern: "active note|open panes|selected text|context"
---

<objective>
Close the workspace-context and composer UAT gaps by making notebook grounding visible and adjustable in the chat surface, and by replacing the truncating single-line composer.

Purpose: satisfy the visible-context half of `APP-02`, keep runtime-backed notebook grounding aligned with `APP-01`, and protect notebook workflow usability required by `APP-03`.
Output: a visible workspace-context control plus a multiline auto-growing composer wired into the shared runtime path.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-UAT.md
@.planning/phases/04-in-app-assistant-parity/04-02-SUMMARY.md
@.planning/phases/04-in-app-assistant-parity/04-04-SUMMARY.md
@App.tsx
@components/App/AppShell.tsx
@components/App/AppWorkspace.tsx
@components/ChatPanel.tsx
@components/ChatPanel/ChatHeader.tsx
@components/ChatPanel/ChatInput.tsx
@src/app/hooks/useAppWorkspaceState.ts
@src/app/hooks/useAIWorkflow.ts
@test/services/inAppAssistantWorkspaceContext.test.ts
@test/components/ChatPanelParity.test.tsx

<interfaces>
From `src/app/hooks/useAppWorkspaceState.ts`:
```typescript
workspaceContext: {
  activeFileId?: string;
  selectedFileIds: string[];
  selectedText?: string;
};
```

From `components/ChatPanel/ChatInput.tsx`:
```typescript
interface ChatInputProps {
  input: string;
  onInputChange: (value: string) => void;
  onSubmit: (event: React.FormEvent) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a visible workspace-context control that drives runtime notebook grounding</name>
  <files>App.tsx, components/App/AppShell.tsx, components/App/AppWorkspace.tsx, components/ChatPanel.tsx, components/ChatPanel/ChatHeader.tsx, components/ChatPanel/WorkspaceContextPanel.tsx, src/app/hooks/useAppWorkspaceState.ts, src/app/hooks/useAIWorkflow.ts, utils/translations.ts, test/components/WorkspaceContextPanel.test.tsx, test/services/inAppAssistantWorkspaceContextControls.test.ts</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - App.tsx
    - components/App/AppShell.tsx
    - components/App/AppWorkspace.tsx
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatHeader.tsx
    - src/app/hooks/useAppWorkspaceState.ts
    - src/app/hooks/useAIWorkflow.ts
    - test/services/inAppAssistantWorkspaceContext.test.ts
  </read_first>
  <behavior>
    - Test 1: The chat surface shows the active note, selected file scope, and selected-text status before submit.
    - Test 2: The user can switch context scope between `Focused note only` and `Open panes`, and can toggle whether highlighted text is injected.
    - Test 3: The runtime request uses the chosen scope and selected-text toggle instead of hidden defaults.
  </behavior>
  <action>Create a dedicated `WorkspaceContextPanel` and a visible header control for it. Thread explicit chat-surface context controls from `App.tsx` through the app shell into `useAIWorkflow.ts`. The controls must use concrete options: `Focused note only`, `Open panes`, and `Include highlighted text`. The panel must show the active note title, selected file count, and a preview of highlighted text when present. Update request assembly so `selectedFileIds`, attachments, and `selectedText` follow the user’s chosen scope rather than always using open-pane defaults.</action>
  <acceptance_criteria>
    - `rg -n "Focused note only|Open panes|Include highlighted text|Active note|Selected text" components/ChatPanel/WorkspaceContextPanel.tsx utils/translations.ts`
    - `rg -n "contextScope|includeSelectedText|workspaceContext" App.tsx src/app/hooks/useAppWorkspaceState.ts src/app/hooks/useAIWorkflow.ts`
    - `bun run vitest run test/services/inAppAssistantWorkspaceContext.test.ts test/services/inAppAssistantWorkspaceContextControls.test.ts test/components/WorkspaceContextPanel.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantWorkspaceContext.test.ts test/services/inAppAssistantWorkspaceContextControls.test.ts test/components/WorkspaceContextPanel.test.tsx</automated>
  </verify>
  <done>The chat drawer visibly shows and controls notebook grounding, and the runtime request matches the user-selected context scope before each send.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the single-line composer with a multiline auto-growing chat input</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatInput.tsx, utils/translations.ts, test/components/ChatInput.test.tsx, test/components/ChatPanelParity.test.tsx</files>
  <read_first>
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatInput.tsx
    - utils/translations.ts
    - test/components/ChatPanelParity.test.tsx
  </read_first>
  <behavior>
    - Test 1: Long prompts wrap across lines and the composer grows vertically instead of clipping text.
    - Test 2: `Enter` submits the prompt and `Shift+Enter` inserts a newline.
    - Test 3: Voice-input append behavior still lands in the multiline composer without regressing send behavior.
  </behavior>
  <action>Replace the current single-line `<input>` with a multiline auto-growing `<textarea>` in `ChatInput.tsx`. Keep the current send button and voice-input affordance, but implement explicit keyboard behavior: `Enter` submits and `Shift+Enter` inserts a newline. Preserve existing injected-message submit flow from `ChatPanel.tsx` and update tests so multiline prompts are visible and submittable.</action>
  <acceptance_criteria>
    - `rg -n "textarea|Shift\\+Enter|Enter" components/ChatPanel/ChatInput.tsx test/components/ChatInput.test.tsx`
    - `bun run vitest run test/components/ChatInput.test.tsx test/components/ChatPanelParity.test.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/ChatInput.test.tsx test/components/ChatPanelParity.test.tsx && bun run tsc --noEmit</automated>
  </verify>
  <done>The in-app assistant composer is multiline, auto-growing, and keyboard-correct, so long prompts remain visible and usable inside the parity chat surface.</done>
</task>

</tasks>

<verification>
Run the new workspace-context and multiline-composer tests, then confirm the chat surface now makes notebook grounding visible and controllable before send.
</verification>

<success_criteria>
Users can see and adjust assistant context from the chat surface, and longer prompts no longer truncate in a single-line composer.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md`
</output>
