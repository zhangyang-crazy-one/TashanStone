---
phase: 04-in-app-assistant-parity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - components/AISettingsModal.tsx
  - src/services/assistant-runtime/settingsCatalog.ts
  - utils/translations.ts
  - test/components/AISettingsModalPresentation.test.tsx
autonomous: true
requirements:
  - APP-01
gap_closure: true
must_haves:
  truths:
    - "The in-app settings modal shows real, usable assistant controls instead of mixing shipped settings with planning-state shell metadata."
    - "Users no longer see Phase 1 or later-phase readiness badges as if they were product features."
    - "Deferred assistant settings remain internal metadata or clearly non-interactive references instead of selectable working controls."
  artifacts:
    - path: "components/AISettingsModal.tsx"
      provides: "User-facing assistant settings modal that presents implemented controls truthfully"
    - path: "src/services/assistant-runtime/settingsCatalog.ts"
      provides: "Availability-aware settings metadata so deferred sections are not rendered as shipped controls"
    - path: "test/components/AISettingsModalPresentation.test.tsx"
      provides: "Regression coverage for implemented vs deferred settings presentation"
  key_links:
    - from: "components/AISettingsModal.tsx"
      to: "src/services/assistant-runtime/settingsCatalog.ts"
      via: "modal rendering must filter or clearly separate deferred metadata from real controls"
      pattern: "phase|deferred|available|planned"
    - from: "components/AISettingsModal.tsx"
      to: "utils/translations.ts"
      via: "user-facing copy should describe actual availability rather than planning phase labels"
      pattern: "assistantSettings|available|planned"
---

<objective>
Close the settings-truthfulness UAT gap so the assistant settings modal stops presenting planning metadata as if it were implemented product surface.

Purpose: restore user trust in the in-app assistant configuration surface and align the visible settings shell with the real implementation shipped by Phase 04.
Output: a user-facing settings modal that shows real controls, hides or clearly separates deferred sections, and has regression coverage for truthful presentation.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-UAT.md
@.planning/phases/04-in-app-assistant-parity/04-01-SUMMARY.md
@components/AISettingsModal.tsx
@src/services/assistant-runtime/settingsCatalog.ts
@utils/translations.ts
@test/services/assistantSettingsPersistence.test.tsx

<interfaces>
From `src/services/assistant-runtime/settingsCatalog.ts`:
```typescript
export interface ResolvedAssistantSettingsSection {
  id: string;
  title: string;
  help: string;
  phase: AssistantSettingsSectionPhase;
  phaseLabel: string;
}
```

From `components/AISettingsModal.tsx`:
```typescript
const settingsShellSurfaces = useMemo(
  () => ASSISTANT_SETTINGS_SURFACE_ORDER.map(surfaceId => resolveAssistantSettingsSurface(currentUiLang, surfaceId)),
  [currentUiLang],
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove planning-state shell copy from the user-facing settings modal</name>
  <files>components/AISettingsModal.tsx, src/services/assistant-runtime/settingsCatalog.ts, utils/translations.ts</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - components/AISettingsModal.tsx
    - src/services/assistant-runtime/settingsCatalog.ts
    - utils/translations.ts
    - test/services/assistantSettingsPersistence.test.tsx
  </read_first>
  <behavior>
    - Test 1: The modal still renders the real tabs and working settings sections already shipped in the product.
    - Test 2: The modal no longer renders "Settings Blueprint", "Phase 1 ready", or "Later phase" as user-facing product messaging.
    - Test 3: Deferred descriptor sections remain non-user-facing metadata or are shown only under explicit planned-state messaging that is not selectable as a live control.
  </behavior>
  <action>Refactor the assistant-settings shell rendering so the default modal experience is truthful. Remove the current blueprint/status card that says the shell is Phase 1 only, stop surfacing raw descriptor phase labels as if they were shipped features, and keep deferred catalog entries out of the normal user path. Preserve `assistantSettings` persistence data and catalog metadata for internal wiring, but do not expose the planning shell picker as a primary product control.</action>
  <acceptance_criteria>
    - `! rg -n "Settings Blueprint|Phase 1 wires the descriptor-backed shell and persistence only|阶段 1 仅接入描述符驱动的设置外壳与持久化|Phase 1 ready|Later phase|第一阶段可用|后续阶段" components/AISettingsModal.tsx utils/translations.ts`
    - `rg -n "AI Config|MCP / Tools|Context|Appearance" components/AISettingsModal.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run tsc --noEmit</automated>
  </verify>
  <done>The modal presents only truthful, usable assistant settings by default, and users are no longer shown planning-state shell controls as if they were implemented product features.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for implemented versus deferred assistant settings presentation</name>
  <files>test/components/AISettingsModalPresentation.test.tsx</files>
  <read_first>
    - components/AISettingsModal.tsx
    - src/services/assistant-runtime/settingsCatalog.ts
    - utils/translations.ts
    - test/services/assistantSettingsPersistence.test.tsx
  </read_first>
  <behavior>
    - Test 1: Rendering the modal shows real settings tabs and implemented controls without any phase-readiness badges.
    - Test 2: Deferred settings descriptors are not exposed as normal interactive controls in the modal body.
    - Test 3: The test fails if planning-state strings or shell selectors reappear in the user-facing modal.
  </behavior>
  <action>Create a focused component test that renders `AISettingsModal` with a persisted assistant config and asserts the user-facing modal contains working tabs while excluding planning-state copy and deferred-shell selectors. Keep persistence tests in `assistantSettingsPersistence.test.tsx` intact; the new test should lock the presentation contract separately.</action>
  <acceptance_criteria>
    - `rg -n "Settings Blueprint|Phase 1 ready|Later phase|AI Config|MCP / Tools" test/components/AISettingsModalPresentation.test.tsx`
    - `bun run vitest run test/components/AISettingsModalPresentation.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/AISettingsModalPresentation.test.tsx</automated>
  </verify>
  <done>Presentation coverage fails if future changes reintroduce misleading planning-state messaging into the assistant settings modal.</done>
</task>

</tasks>

<verification>
Open the modal tests and typecheck. Confirm the settings surface now exposes real controls only and the diagnosed planning-state copy is gone from the visible UI.
</verification>

<success_criteria>
The assistant settings modal is truthful: shipped controls remain available, deferred metadata is not misrepresented, and regression coverage protects the presentation contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-05-SUMMARY.md`
</output>
