---
phase: 04-in-app-assistant-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - App.tsx
  - src/app/hooks/useAIWorkflow.ts
  - src/app/hooks/useAppWorkspaceState.ts
  - test/services/inAppAssistantWorkspaceContext.test.ts
  - test/services/inAppAssistantWorkflowParity.test.ts
autonomous: true
requirements:
  - APP-01
  - APP-03
must_haves:
  truths:
    - "The runtime request built by the in-app assistant reflects the real active notebook workspace instead of placeholder notebook-wide defaults."
    - "Notebook-centric assistant workflows continue to operate through the shared runtime path rather than bypassing it with UI-local logic."
    - "Parity work narrows or structures context assembly intentionally instead of silently attaching the whole notebook to every request."
  artifacts:
    - path: "src/app/hooks/useAIWorkflow.ts"
      provides: "Workspace-faithful runtime request assembly and parity-safe in-app runtime execution"
    - path: "src/app/hooks/useAppWorkspaceState.ts"
      provides: "Real active-note and selection facts that the runtime bridge can consume"
    - path: "test/services/inAppAssistantWorkflowParity.test.ts"
      provides: "Regression coverage for notebook-centric runtime-backed assistant workflows"
  key_links:
    - from: "App.tsx"
      to: "src/app/hooks/useAIWorkflow.ts"
      via: "active pane, selected text, and notebook context must flow from real app state into runtime execution"
      pattern: "activeFile|selectedText|workspace|session"
    - from: "src/app/hooks/useAppWorkspaceState.ts"
      to: "src/app/hooks/useAIWorkflow.ts"
      via: "real workspace state should replace notebook-wide placeholders in the runtime request"
      pattern: "selectedText|getActivePane|active"
---

<objective>
Replace the current placeholder notebook-context bridge with workspace-faithful request assembly and lock notebook-centric assistant workflows onto the shared runtime path.

Purpose: satisfy the runtime-ownership half of `APP-01` and the workflow-compatibility requirement in `APP-03`.
Output: the in-app assistant uses real active-note and selection state, while memory injection, knowledge retrieval, and related app workflows remain usable.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-RESEARCH.md
@.planning/phases/04-in-app-assistant-parity/04-01-PLAN.md
@App.tsx
@src/app/hooks/useAIWorkflow.ts
@src/app/hooks/useAppWorkspaceState.ts
@src/app/hooks/useEditorActions.ts
@components/ChatPanel.tsx
@components/ChatPanel/useChatMemory.ts

<interfaces>
From `src/app/hooks/useAIWorkflow.ts`:
```typescript
activeFileId: filesRef.current[0]?.id,
selectedFileIds: filesRef.current.map(file => file.id),
attachments: notebookAttachments,
```

From `src/app/hooks/useAppWorkspaceState.ts`:
```typescript
interface UseAppWorkspaceStateResult {
  selectedText: string;
  viewRouterProps: AppViewRouterProps;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feed real workspace state into in-app runtime requests</name>
  <files>App.tsx, src/app/hooks/useAIWorkflow.ts, src/app/hooks/useAppWorkspaceState.ts, test/services/inAppAssistantWorkspaceContext.test.ts</files>
  <read_first>
    - App.tsx
    - src/app/hooks/useAIWorkflow.ts
    - src/app/hooks/useAppWorkspaceState.ts
    - src/app/hooks/useEditorActions.ts
    - .planning/phases/04-in-app-assistant-parity/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The runtime request uses the actual active note, relevant selected file ids, and current editor selection instead of placeholder notebook-wide defaults.
    - Test 2: Workspace metadata passed to the runtime remains deterministic and app-owned rather than inferred from arbitrary file ordering.
    - Test 3: Context fidelity improvements do not reintroduce UI-specific runtime execution paths.
  </behavior>
  <action>Thread the real app workspace state into the runtime bridge. Replace the current placeholder `activeFileId`, blanket `selectedFileIds`, and notebook-wide attachment assumptions with app-derived context that matches the active pane and current selection.</action>
  <acceptance_criteria>
    - `rg -n "activeFileId|selectedFileIds|selectedText|workspaceId" App.tsx src/app/hooks/useAIWorkflow.ts src/app/hooks/useAppWorkspaceState.ts`
    - `bun run vitest run test/services/inAppAssistantWorkspaceContext.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantWorkspaceContext.test.ts</automated>
  </verify>
  <done>The in-app runtime request now reflects the real notebook workspace rather than a notebook-wide fallback snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve notebook-centric workflows on the shared runtime path</name>
  <files>src/app/hooks/useAIWorkflow.ts, components/ChatPanel/useChatMemory.ts, test/services/inAppAssistantWorkflowParity.test.ts</files>
  <read_first>
    - src/app/hooks/useAIWorkflow.ts
    - components/ChatPanel/useChatMemory.ts
    - test/services/inAppAssistantRuntimeAdapter.test.ts
    - test/services/inAppSessionRuntimeBridge.test.ts
  </read_first>
  <behavior>
    - Test 1: Memory injection, knowledge retrieval, and session-scoped history still operate when the runtime request becomes workspace-aware.
    - Test 2: Voice-input and standard text-submission flows remain compatible with the runtime-backed in-app assistant.
    - Test 3: The app does not silently bypass the runtime for notebook-specific assistant behavior while parity work lands.
  </behavior>
  <action>Audit and preserve the notebook-first chat workflows that sit around the runtime bridge. Keep the app using the shared runtime for execution, but make sure memory injection, knowledge lookup, and other notebook-centric assistant behaviors still function after the request-shape changes.</action>
  <acceptance_criteria>
    - `rg -n "knowledgeQuery|attachments|messages|buildInjectedMessage" src/app/hooks/useAIWorkflow.ts components/ChatPanel/useChatMemory.ts`
    - `bun run vitest run test/services/inAppAssistantWorkflowParity.test.ts`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantWorkflowParity.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>The in-app assistant remains notebook-first and runtime-backed, with the parity bridge no longer depending on placeholder context shortcuts.</done>
</task>

</tasks>

<verification>
Run the workspace-context and workflow-parity tests, then confirm the app now sends faithful notebook context through the shared runtime without regressing notebook-centric assistant behavior.
</verification>

<success_criteria>
Phase 4 gains a workspace-faithful runtime bridge and preserves the notebook workflows that make the assistant feel native inside TashanStone.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-02-SUMMARY.md`
</output>
