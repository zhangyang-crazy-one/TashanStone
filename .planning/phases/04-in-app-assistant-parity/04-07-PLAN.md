---
phase: 04-in-app-assistant-parity
plan: 07
type: execute
wave: 3
depends_on:
  - 04-06
files_modified:
  - components/ChatPanel.tsx
  - components/ChatPanel/ChatHeader.tsx
  - components/ChatPanel/ChatInput.tsx
  - components/ChatPanel/WorkspaceContextPanel.tsx
  - utils/translations.ts
  - test/components/ChatInput.test.tsx
  - test/components/WorkspaceContextPanel.test.tsx
  - test/components/ChatPanelParity.test.tsx
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
gap_closure: true
must_haves:
  truths:
    - "Users can see exactly which note scope and selected text will be injected into the assistant before they send a prompt."
    - "Users can adjust notebook context scope from the chat surface instead of relying on invisible hook-level defaults."
    - "Long prompts wrap and grow in the composer instead of being clipped inside a single-line input."
    - "Existing send, injected-message, and notebook-centric chat flows keep working after the visible context UI and multiline composer are added."
  artifacts:
    - path: "components/ChatPanel/WorkspaceContextPanel.tsx"
      provides: "Visible chat-surface control for inspecting and adjusting injected notebook context"
    - path: "components/ChatPanel/ChatInput.tsx"
      provides: "Multiline auto-growing composer with explicit submit/newline behavior"
    - path: "test/components/ChatPanelParity.test.tsx"
      provides: "Integrated coverage for visible workspace context plus multiline composer behavior in the chat shell"
  key_links:
    - from: "components/ChatPanel/WorkspaceContextPanel.tsx"
      to: "components/ChatPanel.tsx"
      via: "the panel must reflect live workspace facts and update the threaded context controls created in 04-06"
      pattern: "active note|open panes|selected text|contextScope|includeSelectedText"
    - from: "components/ChatPanel/ChatInput.tsx"
      to: "components/ChatPanel.tsx"
      via: "the multiline composer must preserve submit, injected-message, and voice-append behavior"
      pattern: "textarea|Shift\\+Enter|Enter|inputMessage"
---

<objective>
Close the visible workspace-context half of the Phase 04 UAT gaps by surfacing notebook-context controls in the main chat surface and replacing the truncating single-line composer.

Purpose: finish the user-facing notebook-grounding work for `APP-01` and `APP-02` while protecting notebook workflow usability required by `APP-03`.
Output: visible workspace-context controls, a multiline auto-growing composer, and regression coverage for the integrated chat surface.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-UAT.md
@.planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md
@components/ChatPanel.tsx
@components/ChatPanel/ChatHeader.tsx
@components/ChatPanel/ChatInput.tsx
@components/ChatPanel/WorkspaceContextPanel.tsx
@test/components/ChatInput.test.tsx
@test/components/WorkspaceContextPanel.test.tsx
@test/components/ChatPanelParity.test.tsx

<interfaces>
From `components/ChatPanel/WorkspaceContextPanel.tsx`:
```typescript
interface WorkspaceContextPanelProps {
  workspaceContext: {
    activeFileId?: string;
    selectedFileIds: string[];
    selectedText?: string;
  };
  contextScope: 'active-file' | 'open-files';
  includeSelectedText: boolean;
  onContextScopeChange: (scope: 'active-file' | 'open-files') => void;
  onIncludeSelectedTextChange: (value: boolean) => void;
}
```

From `components/ChatPanel/ChatInput.tsx`:
```typescript
interface ChatInputProps {
  input: string;
  onInputChange: (value: string) => void;
  onSubmit: (event: React.FormEvent) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the visible workspace-context control on top of the 04-06 runtime contract</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatHeader.tsx, components/ChatPanel/WorkspaceContextPanel.tsx, utils/translations.ts, test/components/WorkspaceContextPanel.test.tsx, test/components/ChatPanelParity.test.tsx</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - .planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatHeader.tsx
    - components/ChatPanel/WorkspaceContextPanel.tsx
    - test/components/WorkspaceContextPanel.test.tsx
    - test/components/ChatPanelParity.test.tsx
  </read_first>
  <behavior>
    - Test 1: The chat surface shows the active note, selected file scope, and selected-text status before submit.
    - Test 2: The user can switch context scope between `Focused note only` and `Open panes`, and can toggle whether highlighted text is injected.
    - Test 3: The header clearly separates workspace context controls from memory/runtime controls, and the visible panel writes into the 04-06 context contract rather than inventing local-only state.
  </behavior>
  <action>Implement the visible `WorkspaceContextPanel` and its discoverable entry point in the chat header using the context-control plumbing introduced in 04-06. The panel must show the active note title, selected file count, and a preview of highlighted text when present. Keep the control labels concrete: `Focused note only`, `Open panes`, and `Include highlighted text`. Do not reopen App-shell threading in this plan; consume the existing runtime contract and focus on chat-surface discoverability.</action>
  <acceptance_criteria>
    - `rg -n "Focused note only|Open panes|Include highlighted text|Active note|Selected text" components/ChatPanel/WorkspaceContextPanel.tsx utils/translations.ts`
    - `bun run vitest run test/components/WorkspaceContextPanel.test.tsx test/components/ChatPanelParity.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/WorkspaceContextPanel.test.tsx test/components/ChatPanelParity.test.tsx</automated>
  </verify>
  <done>The chat drawer now makes notebook grounding visible and adjustable before submit, using the shared runtime context contract created in 04-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the single-line composer with a multiline auto-growing chat input</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatInput.tsx, utils/translations.ts, test/components/ChatInput.test.tsx, test/components/ChatPanelParity.test.tsx</files>
  <read_first>
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatInput.tsx
    - utils/translations.ts
    - test/components/ChatInput.test.tsx
    - test/components/ChatPanelParity.test.tsx
  </read_first>
  <behavior>
    - Test 1: Long prompts wrap across lines and the composer grows vertically instead of clipping text.
    - Test 2: `Enter` submits the prompt and `Shift+Enter` inserts a newline.
    - Test 3: Voice-input append and injected-message submission still land in the multiline composer without regressing send behavior.
  </behavior>
  <action>Replace the current single-line `<input>` with a multiline auto-growing `<textarea>` in `ChatInput.tsx`. Keep the current send button and voice-input affordance, but implement explicit keyboard behavior: `Enter` submits and `Shift+Enter` inserts a newline. Preserve the injected-message submit flow from `ChatPanel.tsx`, and update the parity test so workspace-context UI plus multiline input coexist without regressing notebook-centric chat actions.</action>
  <acceptance_criteria>
    - `rg -n "textarea|Shift\\+Enter|Enter" components/ChatPanel/ChatInput.tsx test/components/ChatInput.test.tsx`
    - `bun run vitest run test/components/ChatInput.test.tsx test/components/ChatPanelParity.test.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/ChatInput.test.tsx test/components/ChatPanelParity.test.tsx && bun run tsc --noEmit</automated>
  </verify>
  <done>The in-app assistant composer is multiline, auto-growing, and keyboard-correct, so long prompts remain visible and usable alongside the new workspace-context controls.</done>
</task>

</tasks>

<verification>
Run the workspace-context and multiline-composer component tests. Confirm the chat surface now makes notebook grounding visible before submit and no longer truncates long prompts in a single-line input.
</verification>

<success_criteria>
Phase 04 now has a focused user-facing plan that makes notebook grounding visible and adjustable and restores a usable multiline composer without carrying the session/runtime discoverability work in the same scope.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-07-SUMMARY.md`
</output>
