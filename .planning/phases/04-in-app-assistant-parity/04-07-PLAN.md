---
phase: 04-in-app-assistant-parity
plan: 07
type: execute
wave: 3
depends_on:
  - 04-06
files_modified:
  - components/ChatPanel.tsx
  - components/ChatPanel/ChatHeader.tsx
  - components/ChatPanel/AssistantSessionBar.tsx
  - components/ChatPanel/RuntimeInspectorPanel.tsx
  - components/ChatPanel/WorkspaceContextPanel.tsx
  - utils/translations.ts
  - test/components/AssistantSessionBar.test.tsx
  - test/components/RuntimeInspectorPanel.test.tsx
  - test/components/WorkspaceContextPanel.test.tsx
  - test/components/ChatPanelParity.test.tsx
autonomous: true
requirements:
  - APP-02
gap_closure: true
must_haves:
  truths:
    - "Users can see exactly which note scope and selected text will be injected into the assistant before they send a prompt."
    - "Users can adjust notebook context scope from the chat surface instead of relying on invisible hook-level defaults."
    - "The primary chat surface explains that sessions are isolated threads with separate history and context."
    - "Users can see live runtime activity while a request is streaming without discovering a hidden icon-only control first."
    - "Runtime inspection, memory controls, and notebook context controls are distinct and understandable from the chat header."
  artifacts:
    - path: "components/ChatPanel/WorkspaceContextPanel.tsx"
      provides: "Visible chat-surface control for inspecting and adjusting injected notebook context"
    - path: "components/ChatPanel/AssistantSessionBar.tsx"
      provides: "Discoverable isolated-thread UI with explanatory copy and active-thread affordances"
    - path: "components/ChatPanel/RuntimeInspectorPanel.tsx"
      provides: "Live runtime visibility for lifecycle, streaming deltas, and context-state changes"
    - path: "test/components/ChatPanelParity.test.tsx"
      provides: "Integrated coverage for discoverable thread and runtime visibility in the final chat shell"
  key_links:
    - from: "components/ChatPanel/WorkspaceContextPanel.tsx"
      to: "components/ChatPanel.tsx"
      via: "the panel must reflect live workspace facts and update the threaded context controls created in 04-06"
      pattern: "active note|open panes|selected text|contextScope|includeSelectedText"
    - from: "components/ChatPanel/AssistantSessionBar.tsx"
      to: "src/app/hooks/useAssistantSessions.ts"
      via: "the visible thread copy must still use canonical session ids and selection callbacks"
      pattern: "sessionId|active|create"
    - from: "components/ChatPanel.tsx"
      to: "components/ChatPanel/RuntimeInspectorPanel.tsx"
      via: "live runtime state should auto-surface during active runs and remain manually inspectable afterward"
      pattern: "showRuntimeInspector|lifecyclePhase|streamDeltaCount"
---

<objective>
Close the remaining discoverability gaps by surfacing notebook context controls in the main chat surface, making isolated threads understandable, and making runtime streaming state visibly live during execution.

Purpose: finish the user-facing `APP-02` parity work so notebook context, session isolation, and runtime inspection are understandable without prior product knowledge or hidden toggles.
Output: visible workspace-context controls, clearer isolated-thread UX, stronger runtime visibility during streaming, and regression coverage for the integrated discoverability surface.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-UAT.md
@.planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md
@.planning/phases/04-in-app-assistant-parity/04-03-SUMMARY.md
@.planning/phases/04-in-app-assistant-parity/04-04-SUMMARY.md
@components/ChatPanel.tsx
@components/ChatPanel/ChatHeader.tsx
@components/ChatPanel/AssistantSessionBar.tsx
@components/ChatPanel/RuntimeInspectorPanel.tsx
@components/ChatPanel/WorkspaceContextPanel.tsx
@test/components/WorkspaceContextPanel.test.tsx
@test/components/AssistantSessionBar.test.tsx
@test/components/RuntimeInspectorPanel.test.tsx
@test/components/ChatPanelParity.test.tsx

<interfaces>
From `components/ChatPanel/WorkspaceContextPanel.tsx`:
```typescript
interface WorkspaceContextPanelProps {
  workspaceContext: {
    activeFileId?: string;
    selectedFileIds: string[];
    selectedText?: string;
  };
  contextScope: 'active-file' | 'open-files';
  includeSelectedText: boolean;
  onContextScopeChange: (scope: 'active-file' | 'open-files') => void;
  onIncludeSelectedTextChange: (value: boolean) => void;
}
```

From `components/ChatPanel/AssistantSessionBar.tsx`:
```typescript
interface AssistantSessionBarProps {
  sessions: AssistantSessionRecord[];
  activeSessionId: string | null;
  onCreateSession: () => Promise<unknown> | void;
  onSelectSession: (sessionId: string) => Promise<void> | void;
}
```

From `components/ChatPanel/RuntimeInspectorPanel.tsx`:
```typescript
interface RuntimeInspectorPanelProps {
  inspection: AssistantRuntimeInspectionState;
  language?: Language;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the visible workspace-context control on top of the 04-06 runtime contract</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatHeader.tsx, components/ChatPanel/WorkspaceContextPanel.tsx, utils/translations.ts, test/components/WorkspaceContextPanel.test.tsx, test/components/ChatPanelParity.test.tsx</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - .planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatHeader.tsx
    - components/ChatPanel/WorkspaceContextPanel.tsx
    - test/components/WorkspaceContextPanel.test.tsx
    - test/components/ChatPanelParity.test.tsx
  </read_first>
  <behavior>
    - Test 1: The chat surface shows the active note, selected file scope, and selected-text status before submit.
    - Test 2: The user can switch context scope between `Focused note only` and `Open panes`, and can toggle whether highlighted text is injected.
    - Test 3: The header clearly separates workspace context controls from memory/runtime controls, and the visible panel writes into the 04-06 context contract rather than inventing local-only state.
  </behavior>
  <action>Implement the visible `WorkspaceContextPanel` and its discoverable entry point in the chat header using the context-control plumbing introduced in 04-06. The panel must show the active note title, selected file count, and a preview of highlighted text when present. Keep the control labels concrete: `Focused note only`, `Open panes`, and `Include highlighted text`. Do not reopen App-shell threading in this plan; consume the existing runtime contract and focus on chat-surface discoverability.</action>
  <acceptance_criteria>
    - `rg -n "Focused note only|Open panes|Include highlighted text|Active note|Selected text" components/ChatPanel/WorkspaceContextPanel.tsx utils/translations.ts`
    - `bun run vitest run test/components/WorkspaceContextPanel.test.tsx test/components/ChatPanelParity.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/WorkspaceContextPanel.test.tsx test/components/ChatPanelParity.test.tsx</automated>
  </verify>
  <done>The chat drawer now makes notebook grounding visible and adjustable before submit, using the shared runtime context contract created in 04-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make isolated-thread session UX self-explanatory in the main chat surface</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatHeader.tsx, components/ChatPanel/AssistantSessionBar.tsx, utils/translations.ts, test/components/AssistantSessionBar.test.tsx, test/components/ChatPanelParity.test.tsx</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatHeader.tsx
    - components/ChatPanel/AssistantSessionBar.tsx
    - test/components/AssistantSessionBar.test.tsx
    - test/components/ChatPanelParity.test.tsx
  </read_first>
  <behavior>
    - Test 1: The session strip is labeled as isolated threads or equivalent user-facing copy instead of an unexplained generic sessions label.
    - Test 2: The UI explains that each thread keeps separate history and context, and clearly marks which thread is active.
    - Test 3: Creating or switching threads still uses the canonical callbacks without introducing local-only thread state.
  </behavior>
  <action>Rework the session copy and affordances so isolation is discoverable from the default chat surface. Replace the generic `Sessions` framing with explicit thread/isolation wording, add a short explanatory line such as `Each thread keeps separate history and context`, mark the active thread clearly, and keep the create/switch actions in the visible strip instead of relying on users to infer what session cards mean.</action>
  <acceptance_criteria>
    - `rg -n "thread|isolated|separate history|separate context|active thread" components/ChatPanel/AssistantSessionBar.tsx components/ChatPanel/ChatHeader.tsx utils/translations.ts`
    - `bun run vitest run test/components/AssistantSessionBar.test.tsx test/components/ChatPanelParity.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/AssistantSessionBar.test.tsx test/components/ChatPanelParity.test.tsx</automated>
  </verify>
  <done>The chat drawer makes thread isolation understandable at a glance while staying on the canonical session model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface live runtime activity without requiring a hidden icon toggle</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatHeader.tsx, components/ChatPanel/RuntimeInspectorPanel.tsx, utils/translations.ts, test/components/RuntimeInspectorPanel.test.tsx, test/components/ChatPanelParity.test.tsx</files>
  <read_first>
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatHeader.tsx
    - components/ChatPanel/RuntimeInspectorPanel.tsx
    - src/app/hooks/useAssistantRuntimeInspection.ts
    - test/components/RuntimeInspectorPanel.test.tsx
    - test/components/ChatPanelParity.test.tsx
  </read_first>
  <behavior>
    - Test 1: A labeled runtime control or live status strip is visible in the header, not only an icon-only affordance.
    - Test 2: The runtime panel auto-surfaces or visibly updates while lifecycle phase is queued, assembling, streaming, or processing tools.
    - Test 3: Users can see live phase, delta count, and last-update details during streaming without opening devtools.
  </behavior>
  <action>Upgrade runtime visibility from a hidden Eye button to a discoverable live-status control. Add explicit runtime copy in the header, show live phase and delta/status pills while a request is active, and auto-expand or prominently reveal `RuntimeInspectorPanel` during active runs. Keep the panel read-only, but emphasize live updates with concrete fields such as current phase, delta count, last delta preview, and last updated time.</action>
  <acceptance_criteria>
    - `rg -n "Live runtime|Runtime|delta|last update|streaming|queued|processing" components/ChatPanel/ChatHeader.tsx components/ChatPanel/RuntimeInspectorPanel.tsx utils/translations.ts`
    - `bun run vitest run test/components/RuntimeInspectorPanel.test.tsx test/components/ChatPanelParity.test.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/RuntimeInspectorPanel.test.tsx test/components/ChatPanelParity.test.tsx && bun run tsc --noEmit</automated>
  </verify>
  <done>Runtime state is visibly live during streaming and remains understandable from the chat surface without any hidden control discovery step.</done>
</task>

</tasks>

<verification>
Run the integrated chat, workspace-context, session-bar, and runtime-inspector component tests. Confirm the final chat surface now makes notebook grounding, isolated threads, and live runtime execution obvious to a first-time user.
</verification>

<success_criteria>
Phase 04 gap closure ends with a chat surface that truthfully explains notebook context and thread isolation and visibly shows live runtime activity while preserving the shared-runtime architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-07-SUMMARY.md`
</output>
