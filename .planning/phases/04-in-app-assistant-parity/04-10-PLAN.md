---
phase: 04-in-app-assistant-parity
plan: 10
type: execute
wave: 6
depends_on:
  - 04-09
files_modified:
  - tui-notebook/src/action.rs
  - tui-notebook/src/app.rs
  - tui-notebook/src/components/chat.rs
  - tui-notebook/src/i18n.rs
  - tui-notebook/src/services/ai.rs
  - tui-notebook/tests/test_ai_streaming.rs
  - tui-notebook/tests/test_chat_panel.rs
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
gap_closure: true
must_haves:
  truths:
    - "The TUI chat surface exposes an active conversation plus isolated-thread switching instead of trapping the user in one implicit transcript."
    - "The TUI chat surface shows incremental assistant output with visible running or streaming status and a working stop control when streaming is enabled."
    - "The TUI composer supports multiline entry and discoverable clear, compact, and stop controls without blocking notebook editing or knowledge workflows."
  artifacts:
    - path: "tui-notebook/src/components/chat.rs"
      provides: "Session-aware TUI chat surface with multiline composer, visible controls, and runtime status rendering"
    - path: "tui-notebook/src/app.rs"
      provides: "Async orchestration for session switching, stream cancellation, transcript compaction, and notebook-safe chat execution"
    - path: "tui-notebook/src/services/ai.rs"
      provides: "Streaming-capable AI transport layer for the TUI notebook client"
    - path: "tui-notebook/tests/test_chat_panel.rs"
      provides: "Interaction coverage for thread switching, multiline input, clear or compact controls, and stop behavior"
    - path: "tui-notebook/tests/test_ai_streaming.rs"
      provides: "Transport and parsing coverage for streaming delivery and graceful fallback behavior"
  key_links:
    - from: "tui-notebook/src/components/chat.rs"
      to: "tui-notebook/src/app.rs"
      via: "new chat actions must route session selection, cancellation, clear, and compaction through the app event loop instead of local-only placeholders"
      pattern: "ChatAction::CreateSession|ChatAction::SelectSession|ChatAction::Cancel|ChatAction::Clear|ChatAction::Compact"
    - from: "tui-notebook/src/app.rs"
      to: "tui-notebook/src/services/ai.rs"
      via: "the app must call a real streaming transport and forward start, delta, finish, and error events into the TUI chat state"
      pattern: "chat_streaming|StreamResponse|Cancel"
    - from: "tui-notebook/src/components/chat.rs"
      to: "tui-notebook/src/services/config.rs"
      via: "chat-session visibility and streaming behavior must respect the configured session policy and streaming toggle rather than ignoring saved settings"
      pattern: "session_policy|streaming_enabled"
---

<objective>
Close the reopened Phase 04 gaps on the real `tui-notebook` chat surface by implementing isolated-thread controls, true visible streaming, and a multiline control-rich composer.

Purpose: align the terminal notebook client with the same in-app parity promises already claimed by the desktop React surface, so Phase 04 is truthful across the notebook-native UI that the user is actually exercising.
Output: a TUI chat panel that can switch isolated conversations, stream visibly, stop or clear or compact transcript state, and accept multiline prompts without clipping.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-in-app-assistant-parity/04-UAT.md
@tui-notebook/Cargo.toml
@tui-notebook/USER_GUIDE.md
@tui-notebook/src/action.rs
@tui-notebook/src/app.rs
@tui-notebook/src/components/chat.rs
@tui-notebook/src/i18n.rs
@tui-notebook/src/services/ai.rs
@tui-notebook/src/services/config.rs
@tui-notebook/src/components/settings.rs

<interfaces>
From `tui-notebook/src/action.rs`:
```rust
pub enum ChatAction {
    Send(String),
    Cancel,
    Clear,
    LoadHistory,
    StreamResponse(String),
    SetModel(ChatModel),
}
```

From `tui-notebook/src/components/chat.rs`:
```rust
pub struct ChatPanel {
    messages: VecDeque<ChatMessage>,
    input_buffer: Rope,
    is_streaming: bool,
    model: ChatModel,
    is_open: bool,
    language: Language,
}
```

Current blockers:
```rust
// tui-notebook/src/components/chat.rs
match key.code {
    KeyCode::Enter => { /* always submit */ }
    KeyCode::Char(c) => { /* append single line */ }
}

// tui-notebook/src/services/ai.rs
pub async fn chat_streaming(...) -> Result<Box<dyn tokio::io::AsyncRead + Send>, AiError> {
    Err(AiError::Config("Streaming not implemented".to_string()))
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make isolated TUI conversations discoverable and switchable</name>
  <files>tui-notebook/src/action.rs, tui-notebook/src/app.rs, tui-notebook/src/components/chat.rs, tui-notebook/src/i18n.rs, tui-notebook/tests/test_chat_panel.rs</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - tui-notebook/src/action.rs
    - tui-notebook/src/app.rs
    - tui-notebook/src/components/chat.rs
    - tui-notebook/src/services/config.rs
    - tui-notebook/src/components/settings.rs
    - tui-notebook/USER_GUIDE.md
  </read_first>
  <behavior>
    - Test 1: The chat header shows the active conversation name plus whether it is main or isolated, instead of an unlabeled single transcript.
    - Test 2: Users can create and switch isolated conversations from the TUI chat surface, and each session preserves its own message history.
    - Test 3: Session visibility respects `main_and_isolated` versus `direct_only` policy, so settings do not advertise isolation while the chat panel ignores them.
  </behavior>
  <action>Introduce a session-aware TUI chat state instead of one global `messages` deque. Extend `ChatAction` with explicit session management operations, add visible thread metadata and shortcut hints to the chat header, and let users create or cycle conversations without leaving the chat panel. Keep the implementation notebook-native: use the existing config session policy values (`main_and_isolated`, `workspace_scoped`, `direct_only`) to decide whether isolated-thread affordances are shown, and store session-specific history in the chat component or app state rather than in ad-hoc globals. Add tests that prove switching sessions preserves distinct histories and that the UI clearly marks which thread is active.</action>
  <acceptance_criteria>
    - `rg -n "CreateSession|SelectSession|main_and_isolated|direct_only|isolated|active session|active thread" tui-notebook/src/action.rs tui-notebook/src/components/chat.rs tui-notebook/src/app.rs tui-notebook/src/i18n.rs`
    - `cargo test --manifest-path tui-notebook/Cargo.toml chat_session`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path tui-notebook/Cargo.toml chat_session</automated>
  </verify>
  <done>The TUI chat surface exposes real session switching with discoverable active-thread state and no longer traps the user in one implicit conversation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement real TUI streaming delivery with visible runtime state and stop control</name>
  <files>tui-notebook/src/action.rs, tui-notebook/src/app.rs, tui-notebook/src/components/chat.rs, tui-notebook/src/i18n.rs, tui-notebook/src/services/ai.rs, tui-notebook/tests/test_ai_streaming.rs, tui-notebook/tests/test_chat_panel.rs</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-UAT.md
    - tui-notebook/src/services/ai.rs
    - tui-notebook/src/app.rs
    - tui-notebook/src/components/chat.rs
    - tui-notebook/src/services/config.rs
    - tui-notebook/Cargo.toml
  </read_first>
  <behavior>
    - Test 1: When streaming is enabled, assistant output arrives incrementally and visibly updates the active assistant bubble instead of appearing only at the end.
    - Test 2: The chat header or status strip shows live runtime state such as connecting, streaming, stopped, or failed, so the user can tell what the model is doing.
    - Test 3: A stop command cancels the active stream safely, leaves the partial response visible, and does not corrupt notebook editing or later chat requests.
  </behavior>
  <action>Replace the placeholder `chat_streaming()` error path with a streaming-capable transport abstraction in `tui-notebook/src/services/ai.rs`. Use provider-aware implementations where practical, and when a provider cannot stream natively yet, degrade to an explicit fallback path that still emits incremental delivery/status events instead of silently pretending streaming is on. Update `App` so an active chat request owns a cancellable background task and forwards start, delta, finish, cancel, and failure events into the chat component. Update the chat renderer to surface runtime status directly in the header or a dedicated status line, and wire a stop control that aborts only the active chat stream.</action>
  <acceptance_criteria>
    - `rg -n "chat_streaming|StreamStarted|StreamFinished|StreamFailed|Cancel|streaming_enabled" tui-notebook/src/services/ai.rs tui-notebook/src/action.rs tui-notebook/src/app.rs tui-notebook/src/components/chat.rs`
    - `cargo test --manifest-path tui-notebook/Cargo.toml ai_stream`
    - `cargo test --manifest-path tui-notebook/Cargo.toml chat_stream`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path tui-notebook/Cargo.toml ai_stream && cargo test --manifest-path tui-notebook/Cargo.toml chat_stream</automated>
  </verify>
  <done>The TUI notebook client shows genuinely incremental assistant delivery with explicit runtime state and a working stop control instead of a dead streaming toggle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace the single-line TUI composer with a multiline control-rich input</name>
  <files>tui-notebook/src/action.rs, tui-notebook/src/app.rs, tui-notebook/src/components/chat.rs, tui-notebook/src/i18n.rs, tui-notebook/tests/test_chat_panel.rs</files>
  <read_first>
    - tui-notebook/Cargo.toml
    - tui-notebook/src/components/chat.rs
    - tui-notebook/src/app.rs
    - tui-notebook/src/action.rs
    - tui-notebook/USER_GUIDE.md
  </read_first>
  <behavior>
    - Test 1: The composer accepts multiline content with `Shift+Enter` newline and `Enter` submit, and the input area grows to show wrapped text instead of clipping it to one row.
    - Test 2: The chat surface exposes discoverable control hints for clear, compact, and stop, so the user can see those capabilities from the TUI screen itself.
    - Test 3: Clear removes the current session transcript, compact condenses older turns into a synthetic summary while keeping recent turns, and neither operation blocks notebook editing or knowledge search.
  </behavior>
  <action>Replace the current `Rope + Paragraph` single-line composer with a multiline input implementation, preferably by using the already-installed `ratatui-textarea` crate. Preserve terminal-friendly keyboard behavior: `Enter` submits, `Shift+Enter` inserts a newline, and the cursor position must track wrapped rows correctly. Add visible footer or header hints for at least clear, compact, new-thread, and stop actions. Implement `Clear` and a new `Compact` action so the TUI can intentionally reduce transcript clutter without deleting the whole session; compact should deterministically condense older turns into a system summary entry and keep the most recent exchange visible.</action>
  <acceptance_criteria>
    - `rg -n "ratatui_textarea|Shift\\+Enter|Compact|Clear|Stop|new thread|textarea" tui-notebook/src/components/chat.rs tui-notebook/src/action.rs tui-notebook/src/i18n.rs`
    - `cargo test --manifest-path tui-notebook/Cargo.toml multiline_chat`
    - `cargo check --manifest-path tui-notebook/Cargo.toml`
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path tui-notebook/Cargo.toml multiline_chat && cargo check --manifest-path tui-notebook/Cargo.toml</automated>
  </verify>
  <done>The TUI chat composer is multiline and discoverable, and the user can clear, compact, or stop a conversation from the terminal surface without guessing hidden controls.</done>
</task>

</tasks>

<verification>
Run targeted TUI notebook tests for sessions, streaming, and multiline chat controls, then run `cargo check` for the crate. The combined verification must prove that the actual terminal chat surface now supports isolated conversations, visible streaming, stop control, and multiline input rather than only the React shell doing so.
</verification>

<success_criteria>
Phase 04 is only considered re-closed when the real `tui-notebook` chat surface provides isolated-thread switching, visible incremental streaming, multiline input, and clear or compact or stop controls that match the user's manual UAT expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-10-SUMMARY.md`
</output>
