---
phase: 04-in-app-assistant-parity
plan: 01
type: execute
wave: 1
depends_on:
  - 02-04
  - 03-04
files_modified:
  - src/services/assistant-runtime/types.ts
  - src/services/assistant-runtime/createAssistantRuntime.ts
  - src/app/hooks/useAIWorkflow.ts
  - src/app/hooks/useAssistantRuntimeInspection.ts
  - test/services/assistantRuntimeInspection.contracts.test.ts
  - test/services/inAppAssistantInspectionBridge.test.ts
autonomous: true
requirements:
  - APP-01
  - APP-02
must_haves:
  truths:
    - "The in-app assistant exposes an app-facing inspection seam for session, lifecycle, streaming, and assembled-context state without moving execution ownership back into the UI."
    - "Runtime inspection data is defined as transport-neutral metadata compatible with future channel callers rather than ChatPanel-specific state."
    - "The app can observe what the runtime assembled and which session it used without scraping rendered chat text."
  artifacts:
    - path: "src/services/assistant-runtime/types.ts"
      provides: "Transport-neutral runtime inspection metadata and event/result contracts"
    - path: "src/app/hooks/useAssistantRuntimeInspection.ts"
      provides: "Dedicated app-facing inspection state seam separate from AIState and ChatMessage"
    - path: "test/services/inAppAssistantInspectionBridge.test.ts"
      provides: "Regression coverage for runtime inspection state flowing into the app bridge"
  key_links:
    - from: "src/services/assistant-runtime/createAssistantRuntime.ts"
      to: "src/app/hooks/useAssistantRuntimeInspection.ts"
      via: "runtime events and result metadata must be consumable as inspection state without UI-owned recomputation"
      pattern: "lifecycle|stream|context|metadata|session"
    - from: "src/app/hooks/useAIWorkflow.ts"
      to: "src/services/assistant-runtime/types.ts"
      via: "the app bridge should consume canonical inspection contracts rather than ad-hoc local objects"
      pattern: "inspection|session|phase|context"
---

<objective>
Define the canonical inspection seam that Phase 4 will build on so the in-app assistant can expose session, streaming, and assembled-context state while staying on the shared runtime.

Purpose: establish the transport-neutral observability contract required by `APP-02` and keep `APP-01` runtime ownership intact.
Output: runtime inspection metadata/contracts plus an app-facing hook or state seam that consumes them without overloading `AIState`.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-RESEARCH.md
@.planning/phases/02-session-routing-and-persistence/02-04-PLAN.md
@.planning/phases/03-tools-and-multimodal-delivery/03-04-PLAN.md
@src/services/assistant-runtime/types.ts
@src/services/assistant-runtime/createAssistantRuntime.ts
@src/app/hooks/useAIWorkflow.ts
@types.ts

<interfaces>
From `src/services/assistant-runtime/types.ts`:
```typescript
export type AssistantRuntimeEvent =
  | AssistantRuntimeLifecycleEvent
  | AssistantRuntimeStreamDeltaEvent
  | AssistantRuntimeToolStatusEvent
  | AssistantRuntimeResultEvent
  | AssistantRuntimeErrorEvent
  | AssistantRuntimeMediaStatusEvent;
```

From `types.ts`:
```typescript
export interface AIState {
  isThinking: boolean;
  error: string | null;
  message: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define transport-neutral runtime inspection contracts</name>
  <files>src/services/assistant-runtime/types.ts, src/services/assistant-runtime/createAssistantRuntime.ts, test/services/assistantRuntimeInspection.contracts.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/types.ts
    - src/services/assistant-runtime/createAssistantRuntime.ts
    - src/services/assistant-runtime/contextAdapters.ts
    - .planning/phases/04-in-app-assistant-parity/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Runtime contracts expose inspectable session, lifecycle, and assembled-context metadata without embedding React or chat-panel types.
    - Test 2: Inspection metadata remains compatible with non-app callers by living on canonical runtime events or result metadata.
    - Test 3: Context inspection can reference assembled sections and summaries without requiring callers to reconstruct prompt state from rendered output text.
  </behavior>
  <action>Add the missing runtime contract layer for parity inspection so callers can observe session identity, lifecycle phase, streaming progress, and context-section summaries in a transport-neutral way. Keep the change additive and canonical rather than inventing a ChatPanel-only format.</action>
  <acceptance_criteria>
    - `rg -n "inspection|context|session|phase|metadata" src/services/assistant-runtime/types.ts src/services/assistant-runtime/createAssistantRuntime.ts`
    - `bun run vitest run test/services/assistantRuntimeInspection.contracts.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantRuntimeInspection.contracts.test.ts</automated>
  </verify>
  <done>The runtime emits or returns enough canonical metadata for the in-app surface to inspect session, streaming, and assembled-context state without UI-owned guesswork.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bridge runtime inspection state into the app without bloating AIState</name>
  <files>src/app/hooks/useAIWorkflow.ts, src/app/hooks/useAssistantRuntimeInspection.ts, test/services/inAppAssistantInspectionBridge.test.ts</files>
  <read_first>
    - src/app/hooks/useAIWorkflow.ts
    - types.ts
    - test/services/inAppAssistantRuntimeAdapter.test.ts
    - test/services/inAppSessionRuntimeBridge.test.ts
  </read_first>
  <behavior>
    - Test 1: The app bridge captures canonical runtime inspection data while keeping execution inside `useAIWorkflow.ts`.
    - Test 2: `AIState` remains a small thinking/error/message surface instead of becoming the parity state store.
    - Test 3: Session id, lifecycle phase, and context summaries remain readable to later UI plans without parsing chat content or tool cards.
  </behavior>
  <action>Introduce a dedicated app-facing inspection state seam and wire `useAIWorkflow.ts` to update it from runtime events/results. Do not expand `ChatMessage` or `AIState` into generic debug state; keep inspection data explicit and separately testable.</action>
  <acceptance_criteria>
    - `rg -n "inspection|session|lifecycle|context" src/app/hooks/useAIWorkflow.ts src/app/hooks/useAssistantRuntimeInspection.ts`
    - `bun run vitest run test/services/inAppAssistantInspectionBridge.test.ts`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantInspectionBridge.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>The app has a stable inspection seam for later parity UI work, and runtime ownership remains outside the chat components.</done>
</task>

</tasks>

<verification>
Run the inspection-contract and app-bridge tests, then confirm Phase 4 now has a canonical parity-observability seam instead of implicit UI-owned state.
</verification>

<success_criteria>
Phase 4 starts with inspectable runtime/session/context contracts and an app-facing bridge that preserve the shared-runtime architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-01-SUMMARY.md`
</output>
