---
phase: 04-in-app-assistant-parity
plan: 09
type: execute
wave: 5
depends_on:
  - 04-08
files_modified:
  - components/App/AppWorkspace.tsx
  - components/ChatPanel.tsx
  - components/ChatPanel/WorkspaceContextPanel.tsx
  - test/components/AppChatContextThreading.test.tsx
  - test/components/WorkspaceContextPanel.test.tsx
  - test/components/ChatPanelParity.test.tsx
autonomous: true
requirements:
  - APP-02
gap_closure: true
must_haves:
  truths:
    - "The visible workspace-context panel shows the active note using a human-readable title instead of an internal file id."
    - "The app shell threads the current active note title into the chat surface instead of dropping it before the workspace-context panel."
    - "Regression coverage fails if the chat shell or workspace-context panel regresses back to rendering opaque file ids as the visible active-note label."
  artifacts:
    - path: "components/App/AppWorkspace.tsx"
      provides: "App-shell threading of the real active note title into the chat surface"
    - path: "components/ChatPanel.tsx"
      provides: "Chat-surface contract that passes the active note title into WorkspaceContextPanel"
    - path: "test/components/ChatPanelParity.test.tsx"
      provides: "Visible UI regression coverage proving the workspace-context surface shows note title rather than file id"
  key_links:
    - from: "components/App/AppWorkspace.tsx"
      to: "components/ChatPanel.tsx"
      via: "the existing activeFileName prop must be forwarded into the chat surface per the verification gap"
      pattern: "activeFileName"
    - from: "components/ChatPanel.tsx"
      to: "components/ChatPanel/WorkspaceContextPanel.tsx"
      via: "the visible panel must receive the human-readable note title instead of workspaceContext.activeFileId"
      pattern: "activeFileName"
---

<objective>
Close the last Phase 04 truthfulness gap by making the visible workspace-context panel show the real active note title instead of the active file id.

Purpose: finish the blocked `APP-02` verification item from `04-VERIFICATION.md` without reopening broader session, runtime, or composer scope.
Output: corrected app-shell-to-chat active-note-title threading and regression coverage that proves the visible UI shows note title rather than file id.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-VERIFICATION.md
@.planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md
@.planning/phases/04-in-app-assistant-parity/04-07-SUMMARY.md
@.planning/phases/04-in-app-assistant-parity/04-08-SUMMARY.md
@components/App/AppWorkspace.tsx
@components/ChatPanel.tsx
@components/ChatPanel/WorkspaceContextPanel.tsx
@test/components/AppChatContextThreading.test.tsx
@test/components/WorkspaceContextPanel.test.tsx
@test/components/ChatPanelParity.test.tsx

<interfaces>
From `components/App/AppWorkspace.tsx`:
```typescript
interface AppWorkspaceProps {
  activeFileName: string;
  workspaceContext: AssistantWorkspaceContext;
}
```

From `components/ChatPanel/WorkspaceContextPanel.tsx`:
```typescript
interface WorkspaceContextPanelProps {
  workspaceContext: {
    activeFileId?: string;
    selectedFileIds: string[];
    selectedText?: string;
  };
  activeFileName?: string;
}
```

Current blocker in `components/ChatPanel.tsx`:
```typescript
<WorkspaceContextPanel
  workspaceContext={workspaceContext}
  activeFileName={workspaceContext.activeFileId}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread the real active note title from AppWorkspace into the visible workspace-context panel</name>
  <files>components/App/AppWorkspace.tsx, components/ChatPanel.tsx, components/ChatPanel/WorkspaceContextPanel.tsx, test/components/AppChatContextThreading.test.tsx</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-VERIFICATION.md
    - .planning/phases/04-in-app-assistant-parity/04-06-SUMMARY.md
    - components/App/AppWorkspace.tsx
    - components/ChatPanel.tsx
    - components/ChatPanel/WorkspaceContextPanel.tsx
    - test/components/AppChatContextThreading.test.tsx
  </read_first>
  <behavior>
    - Test 1: `AppWorkspace` forwards its existing `activeFileName` prop into `ChatPanel` instead of dropping the value at the app-shell boundary.
    - Test 2: `ChatPanel` passes the forwarded note title into `WorkspaceContextPanel` instead of substituting `workspaceContext.activeFileId`.
    - Test 3: `WorkspaceContextPanel` treats `activeFileName` as the visible active-note label and no longer depends on an opaque file id when the chat shell already knows the note title.
  </behavior>
  <action>Implement the exact gap fix described in `04-VERIFICATION.md`: forward the real `activeFileName` from `AppWorkspace` into `ChatPanel`, add the corresponding `ChatPanel` prop, and pass that prop through to `WorkspaceContextPanel` per `APP-02`. Keep the panel contract centered on a human-readable label and do not re-derive the visible active note from `workspaceContext.activeFileId` inside `ChatPanel`. Update the threading test so it proves the app shell passes the real note title through the chat surface rather than only passing ids.</action>
  <acceptance_criteria>
    - `rg -n "activeFileName" components/App/AppWorkspace.tsx components/ChatPanel.tsx components/ChatPanel/WorkspaceContextPanel.tsx test/components/AppChatContextThreading.test.tsx`
    - `bun run vitest run test/components/AppChatContextThreading.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/AppChatContextThreading.test.tsx</automated>
  </verify>
  <done>The app shell and chat surface carry the real note title into the workspace-context panel, closing the broken prop chain identified by the verifier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the visible active-note label with UI regressions that reject file-id fallbacks</name>
  <files>test/components/WorkspaceContextPanel.test.tsx, test/components/ChatPanelParity.test.tsx, components/ChatPanel.tsx, components/ChatPanel/WorkspaceContextPanel.tsx</files>
  <read_first>
    - .planning/phases/04-in-app-assistant-parity/04-VERIFICATION.md
    - components/ChatPanel.tsx
    - components/ChatPanel/WorkspaceContextPanel.tsx
    - test/components/WorkspaceContextPanel.test.tsx
    - test/components/ChatPanelParity.test.tsx
  </read_first>
  <behavior>
    - Test 1: `WorkspaceContextPanel` renders the supplied note title and not the internal id when both values are present.
    - Test 2: The integrated `ChatPanel` parity test asserts the visible workspace-context surface shows the note title in the final chat shell.
    - Test 3: The visible UI regression fails if `note-1` or another internal id reappears as the active-note label while a human-readable title is available.
  </behavior>
  <action>Extend the visible UI regression slice so the bug cannot come back silently. Update `test/components/WorkspaceContextPanel.test.tsx` to assert the title wins over the id and add an explicit negative assertion against the id appearing as the active-note label. Update `test/components/ChatPanelParity.test.tsx` to render a real note title through the final chat shell and assert that the user-facing panel shows that title rather than `activeFileId`. Keep the scope narrow: this plan exists to lock the active-note-title truthfulness fix, not to revisit other parity behavior.</action>
  <acceptance_criteria>
    - `rg -n "Project Brief|Focused Draft|not.toBeInTheDocument|active note|Active note" test/components/WorkspaceContextPanel.test.tsx test/components/ChatPanelParity.test.tsx`
    - `bun run vitest run test/components/WorkspaceContextPanel.test.tsx test/components/ChatPanelParity.test.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/WorkspaceContextPanel.test.tsx test/components/ChatPanelParity.test.tsx && bun run tsc --noEmit</automated>
  </verify>
  <done>The visible workspace-context UI is covered end-to-end by regressions that prove note titles are shown to users instead of file ids.</done>
</task>

</tasks>

<verification>
Re-run the app-shell threading test plus the workspace-context and chat-shell parity tests. The combined regression slice must prove that the real note title flows from `AppWorkspace` into the visible workspace-context panel and remains visible in the final chat shell.
</verification>

<success_criteria>
Phase 04 has a single focused gap-closure plan that fixes the active-note-title truthfulness bug, satisfies the blocked `APP-02` verification truth, and adds regressions that fail if the panel shows internal file ids again.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-09-SUMMARY.md`
</output>
