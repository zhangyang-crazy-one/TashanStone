---
phase: 04-in-app-assistant-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - components/ChatPanel.tsx
  - components/ChatPanel/ChatHeader.tsx
  - components/ChatPanel/AssistantSessionBar.tsx
  - components/ChatPanel/RuntimeInspectorPanel.tsx
  - components/App/AppWorkspace.tsx
  - test/components/AssistantSessionBar.test.tsx
  - test/components/RuntimeInspectorPanel.test.tsx
autonomous: true
requirements:
  - APP-02
must_haves:
  truths:
    - "Users can see and change the active in-app assistant session from the existing chat surface rather than relying on hidden persistence state."
    - "Users can inspect session state, streaming/lifecycle state, and assembled assistant context from the in-app assistant UI."
    - "The parity UI reads canonical runtime/session data and does not become a second execution owner."
  artifacts:
    - path: "components/ChatPanel/AssistantSessionBar.tsx"
      provides: "Visible session switch/create controls backed by canonical assistant sessions"
    - path: "components/ChatPanel/RuntimeInspectorPanel.tsx"
      provides: "Read-only inspection UI for session, lifecycle, and context state"
    - path: "test/components/RuntimeInspectorPanel.test.tsx"
      provides: "Regression coverage for the Phase 4 parity inspection surface"
  key_links:
    - from: "components/App/AppWorkspace.tsx"
      to: "components/ChatPanel.tsx"
      via: "the workspace must pass parity state and session controls into the existing chat surface"
      pattern: "session|inspection|chat"
    - from: "components/ChatPanel/ChatHeader.tsx"
      to: "components/ChatPanel/RuntimeInspectorPanel.tsx"
      via: "header controls should toggle inspection UI rather than burying state behind implicit behavior"
      pattern: "inspect|session|state"
---

<objective>
Deliver the visible in-app parity surface: session controls plus a runtime inspector that shows session, streaming, and assembled-context state inside the existing chat drawer.

Purpose: satisfy the user-visible inspection requirement in `APP-02`.
Output: session-switch/create UI and a read-only runtime inspector that fit the current in-app assistant shell.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-RESEARCH.md
@.planning/phases/04-in-app-assistant-parity/04-01-PLAN.md
@.planning/phases/04-in-app-assistant-parity/04-02-PLAN.md
@components/ChatPanel.tsx
@components/ChatPanel/ChatHeader.tsx
@components/App/AppWorkspace.tsx
@src/app/hooks/useAssistantSessions.ts
@src/app/hooks/useAssistantRuntimeInspection.ts
@components/ToolCallCard.tsx
@components/StreamToolCard.tsx

<interfaces>
From `components/ChatPanel.tsx`:
```typescript
interface ChatPanelProps {
  messages: ChatMessage[];
  onSendMessage: (text: string) => void;
  aiState: AIState;
}
```

From `src/app/hooks/useAssistantSessions.ts`:
```typescript
interface UseAssistantSessionsResult {
  activeSession: AssistantSessionRecord | null;
  sessions: AssistantSessionRecord[];
  createSession: (input?: CreateAssistantSessionInput) => Promise<AssistantSessionRecord>;
  setActiveSessionId: (sessionId: string) => Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add in-app assistant session controls to the existing chat shell</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatHeader.tsx, components/ChatPanel/AssistantSessionBar.tsx, components/App/AppWorkspace.tsx, test/components/AssistantSessionBar.test.tsx</files>
  <read_first>
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatHeader.tsx
    - components/App/AppWorkspace.tsx
    - src/app/hooks/useAssistantSessions.ts
    - test/services/inAppAssistantSessions.test.ts
  </read_first>
  <behavior>
    - Test 1: The user can see the active session and switch or create sessions from the in-app assistant surface.
    - Test 2: Session controls are driven by canonical session state, not a parallel local tab model.
    - Test 3: The added session UI fits into the existing chat drawer without replacing the established chat flow.
  </behavior>
  <action>Add a session bar or equivalent control surface to the current chat panel so session lifecycle becomes visible and usable inside the app. Reuse the Phase 2 session hooks and keep the UI scoped to parity, not a brand-new assistant shell.</action>
  <acceptance_criteria>
    - `rg -n "session|activeSession|createSession|setActiveSessionId" components/ChatPanel.tsx components/ChatPanel/ChatHeader.tsx components/ChatPanel/AssistantSessionBar.tsx components/App/AppWorkspace.tsx`
    - `bun run vitest run test/components/AssistantSessionBar.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/AssistantSessionBar.test.tsx</automated>
  </verify>
  <done>The in-app assistant visibly supports session switching and creation through the canonical session model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a read-only runtime inspector for session, lifecycle, and context state</name>
  <files>components/ChatPanel.tsx, components/ChatPanel/ChatHeader.tsx, components/ChatPanel/RuntimeInspectorPanel.tsx, test/components/RuntimeInspectorPanel.test.tsx</files>
  <read_first>
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatHeader.tsx
    - src/app/hooks/useAssistantRuntimeInspection.ts
    - components/ToolCallCard.tsx
    - components/StreamToolCard.tsx
  </read_first>
  <behavior>
    - Test 1: The inspector shows session identity and status, current runtime lifecycle or streaming state, and assembled context summaries or sections.
    - Test 2: The inspector remains read-only and does not become a second place where context or execution is mutated.
    - Test 3: Existing tool/media/delivery status cards remain usable alongside the inspector rather than being replaced by it.
  </behavior>
  <action>Build the Phase 4 inspection surface inside the existing chat panel. It should surface session facts, lifecycle or stream progress, and assembled assistant-context details using the canonical inspection seam from Plan 04-01.</action>
  <acceptance_criteria>
    - `rg -n "inspection|context|phase|session|stream" components/ChatPanel.tsx components/ChatPanel/ChatHeader.tsx components/ChatPanel/RuntimeInspectorPanel.tsx`
    - `bun run vitest run test/components/RuntimeInspectorPanel.test.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/RuntimeInspectorPanel.test.tsx && bun run tsc --noEmit</automated>
  </verify>
  <done>The in-app assistant provides a visible parity inspector for session, runtime, and context state without compromising runtime ownership.</done>
</task>

</tasks>

<verification>
Run the session-bar and inspector component tests, then confirm the chat drawer now exposes canonical session and runtime inspection state to users.
</verification>

<success_criteria>
Phase 4 adds a visible parity layer to the existing in-app assistant rather than leaving session/runtime/context state hidden in hooks and storage.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-03-SUMMARY.md`
</output>
