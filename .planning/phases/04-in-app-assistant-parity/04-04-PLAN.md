---
phase: 04-in-app-assistant-parity
plan: 04
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
files_modified:
  - App.tsx
  - components/App/AppWorkspace.tsx
  - components/ChatPanel.tsx
  - components/ChatPanel/useChatMemory.ts
  - test/services/inAppAssistantParityRegression.test.ts
  - test/components/ChatPanelParity.test.tsx
autonomous: true
requirements:
  - APP-01
  - APP-03
must_haves:
  truths:
    - "The parity UI and workspace-aware runtime bridge do not break notebook editing, knowledge retrieval, memory injection, or session-scoped chat history."
    - "The in-app assistant remains a shared-runtime caller even after adding parity UI and workflow compatibility fixes."
    - "Phase 4 ends with targeted regression coverage that proves notebook-centric workflows still work inside the parity-enhanced assistant."
  artifacts:
    - path: "test/services/inAppAssistantParityRegression.test.ts"
      provides: "End-to-end hook/service regression coverage for parity-critical notebook workflows"
    - path: "test/components/ChatPanelParity.test.tsx"
      provides: "Component-level coverage for the final in-app parity surface"
  key_links:
    - from: "App.tsx"
      to: "components/App/AppWorkspace.tsx"
      via: "the app shell must pass the final session and inspection state into the parity-enhanced chat surface without regressing existing workflow wiring"
      pattern: "chat|session|inspection|workspace"
    - from: "components/ChatPanel/useChatMemory.ts"
      to: "src/app/hooks/useAIWorkflow.ts"
      via: "memory injection and runtime-backed assistant submission must remain aligned after parity changes"
      pattern: "memory|inject|message|runtime"
---

<objective>
Close Phase 4 by hardening the final in-app parity surface against notebook-workflow regressions and proving the app still behaves like a notebook-native assistant on top of the shared runtime.

Purpose: complete `APP-03` and lock the parity work under regression coverage while reaffirming `APP-01`.
Output: final compatibility fixes plus targeted regression tests covering notebook-centric in-app assistant behavior.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-in-app-assistant-parity/04-RESEARCH.md
@.planning/phases/04-in-app-assistant-parity/04-02-PLAN.md
@.planning/phases/04-in-app-assistant-parity/04-03-PLAN.md
@App.tsx
@components/App/AppWorkspace.tsx
@components/ChatPanel.tsx
@components/ChatPanel/useChatMemory.ts
@src/app/hooks/useAIWorkflow.ts
@src/app/hooks/useChatHistory.ts
@test/services/inAppAssistantRuntimeAdapter.test.ts
@test/services/inAppAssistantSessions.test.ts

<interfaces>
From `components/ChatPanel.tsx`:
```typescript
onCompactChat?: () => Promise<void>;
showToast?: (message: string, isError?: boolean) => void;
```

From `src/app/hooks/useChatHistory.ts`:
```typescript
replaceSessionMessages(activeSessionId, chatMessages)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix any notebook-workflow regressions introduced by Phase 4 parity wiring</name>
  <files>App.tsx, components/App/AppWorkspace.tsx, components/ChatPanel.tsx, components/ChatPanel/useChatMemory.ts, test/services/inAppAssistantParityRegression.test.ts</files>
  <read_first>
    - App.tsx
    - components/App/AppWorkspace.tsx
    - components/ChatPanel.tsx
    - components/ChatPanel/useChatMemory.ts
    - src/app/hooks/useAIWorkflow.ts
  </read_first>
  <behavior>
    - Test 1: Session-scoped chat history, memory injection, knowledge retrieval, and standard submission still work with the new parity surfaces.
    - Test 2: The final in-app assistant surface still routes execution through the shared runtime rather than a fallback direct AI path.
    - Test 3: Notebook editing and surrounding app interactions remain usable while the parity-enhanced assistant is open.
  </behavior>
  <action>Use the final wave to close any regressions caused by workspace-aware requests, session UI, and the runtime inspector. Keep the focus on preserving notebook-native assistant workflows instead of broad UI redesign.</action>
  <acceptance_criteria>
    - `rg -n "runtime|memory|session|chat|knowledge" App.tsx components/App/AppWorkspace.tsx components/ChatPanel.tsx components/ChatPanel/useChatMemory.ts`
    - `bun run vitest run test/services/inAppAssistantParityRegression.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantParityRegression.test.ts</automated>
  </verify>
  <done>The parity-enhanced in-app assistant preserves the notebook-native workflows it sits inside.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the final in-app parity surface with component and type-safety coverage</name>
  <files>components/ChatPanel.tsx, test/components/ChatPanelParity.test.tsx</files>
  <read_first>
    - components/ChatPanel.tsx
    - components/ChatPanel/ChatHeader.tsx
    - components/ChatPanel/AssistantSessionBar.tsx
    - components/ChatPanel/RuntimeInspectorPanel.tsx
  </read_first>
  <behavior>
    - Test 1: The final chat panel renders session controls, runtime inspection affordances, and the established chat/status flows together.
    - Test 2: The parity surface keeps existing actions such as clear chat, compact chat, and stop streaming accessible.
    - Test 3: Type safety remains green once the chat shell carries the additional parity state.
  </behavior>
  <action>Add focused component-level coverage for the fully integrated chat surface and finish Phase 4 with type-safe parity wiring. This is the final proof that the app now exposes a complete in-app assistant surface on top of the shared runtime.</action>
  <acceptance_criteria>
    - `rg -n "session|inspect|compact|stream|clear" components/ChatPanel.tsx`
    - `bun run vitest run test/components/ChatPanelParity.test.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/ChatPanelParity.test.tsx && bun run tsc --noEmit</automated>
  </verify>
  <done>Phase 4 closes with a regression-locked, type-safe, notebook-native in-app assistant parity surface.</done>
</task>

</tasks>

<verification>
Run the final service and component regression slices, then confirm the app still behaves like a notebook-native assistant while exposing the new parity UI.
</verification>

<success_criteria>
Phase 4 ends with a runtime-backed, inspectable, session-aware in-app assistant that preserves notebook workflows instead of regressing them.
</success_criteria>

<output>
After completion, create `.planning/phases/04-in-app-assistant-parity/04-04-SUMMARY.md`
</output>
