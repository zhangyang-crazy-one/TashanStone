---
phase: 02-session-routing-and-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - electron/database/repositories/sessionRepository.ts
  - electron/ipc/dbHandlers.ts
  - electron/preload.ts
  - src/services/storage/types.ts
  - src/services/storage/electronStorage.ts
  - src/services/storage/webStorage.ts
  - test/services/assistantSessionRepository.test.ts
  - test/services/assistantSessionPersistence.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-04
must_haves:
  truths:
    - "Canonical assistant sessions can be created, listed, resumed, and archived through one repository path."
    - "Session-scoped transcript and reply-context persistence flow through the shared storage layer instead of one global localStorage chat key."
    - "Electron and web storage backends expose the same session-oriented API surface, even if the backing implementation differs."
  artifacts:
    - path: "electron/database/repositories/sessionRepository.ts"
      provides: "Canonical assistant session persistence and transcript association APIs"
    - path: "src/services/storage/types.ts"
      provides: "Shared session-oriented storage contract for app callers"
    - path: "test/services/assistantSessionPersistence.test.ts"
      provides: "Regression coverage for session CRUD, transcript persistence, and reply-context round-tripping"
  key_links:
    - from: "electron/database/repositories/sessionRepository.ts"
      to: "electron/database/repositories/chatRepository.ts"
      via: "canonical session metadata coordinates transcript persistence instead of replacing it"
      pattern: "sessionId|conversationId"
    - from: "src/services/storage/electronStorage.ts"
      to: "electron/ipc/dbHandlers.ts"
      via: "renderer session operations flow through the shared db IPC seam"
      pattern: "db\\.session"
---

<objective>
Implement the shared persistence path for canonical assistant sessions so transcripts, route metadata, and reply context have one durable backend.

Purpose: Satisfy the persistence side of `SESS-01` and `SESS-04` without waiting for the full app or channel phases.
Output: Session repository, shared storage APIs, Electron/web persistence wiring, and regression tests for session lifecycle plus transcript durability.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-routing-and-persistence/02-CONTEXT.md
@.planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
@.planning/phases/02-session-routing-and-persistence/02-01-PLAN.md
@src/services/storage/types.ts
@src/services/storage/electronStorage.ts
@src/services/storage/webStorage.ts
@electron/database/repositories/chatRepository.ts
@electron/ipc/dbHandlers.ts
@electron/preload.ts

<interfaces>
From [src/services/storage/types.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/storage/types.ts):
```typescript
export interface StorageService {
  getChatMessages(conversationId?: string): Promise<ChatMessage[]>;
  addChatMessage(message: ChatMessage, conversationId?: string): Promise<ChatMessage>;
  clearChatMessages(conversationId?: string): Promise<void>;
}
```

From [electron/ipc/dbHandlers.ts](/home/zhangyangrui/my_programes/TashanStone/electron/ipc/dbHandlers.ts):
```typescript
ipcMain.handle('db:chat:getAll', async (_, conversationId?: string) => { ... });
ipcMain.handle('db:chat:add', async (_, message: ChatMessage, conversationId?: string) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the canonical session repository and db IPC seam</name>
  <files>electron/database/repositories/sessionRepository.ts, electron/ipc/dbHandlers.ts, electron/preload.ts, test/services/assistantSessionRepository.test.ts</files>
  <read_first>
    - .planning/phases/02-session-routing-and-persistence/02-01-PLAN.md
    - electron/database/repositories/chatRepository.ts
    - electron/ipc/dbHandlers.ts
    - electron/preload.ts
    - .planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The repository can create or resume canonical sessions and persist route metadata plus reply-context fields.
    - Test 2: The db IPC layer exposes session CRUD and transcript lookup by canonical session id.
    - Test 3: Existing transcript persistence remains reachable through the canonical session path rather than a separate ad hoc API.
  </behavior>
  <action>Per D-02 through D-08, implement a dedicated `sessionRepository.ts` and extend the db IPC bridge plus preload surface with canonical session operations. Reuse the existing chat repository where practical, but make canonical session metadata explicit instead of deriving everything from transcript rows. Keep the Phase 2 scope disciplined: this is repository and IPC substrate work, not a full assistant UI feature.</action>
  <acceptance_criteria>
    - `rg -n "sessionRepository|db:session|assistant session" electron/database/repositories/sessionRepository.ts electron/ipc/dbHandlers.ts electron/preload.ts`
    - `bun run vitest run test/services/assistantSessionRepository.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantSessionRepository.test.ts</automated>
  </verify>
  <done>The main process exposes one canonical session persistence path and app callers can reach it through the shared IPC surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend shared storage services with session-oriented transcript APIs</name>
  <files>src/services/storage/types.ts, src/services/storage/electronStorage.ts, src/services/storage/webStorage.ts, test/services/assistantSessionPersistence.test.ts</files>
  <read_first>
    - src/services/storage/types.ts
    - src/services/storage/electronStorage.ts
    - src/services/storage/webStorage.ts
    - src/app/hooks/useChatHistory.ts
    - .planning/phases/02-session-routing-and-persistence/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Shared storage services can list sessions, load session-scoped transcripts, and persist session metadata through one interface.
    - Test 2: The web fallback keeps the same API shape even if it uses a simplified local implementation.
    - Test 3: Session transcript persistence no longer depends on one implicit global conversation key.
  </behavior>
  <action>Per D-06 through D-09, extend `StorageService` and both storage backends with canonical session-oriented operations. Reconcile the current chat history methods with the new session model and preserve compatibility where reasonable, but move the canonical behavior toward session-scoped transcript loading and saving. Do not wire app hooks here; Phase 2's app integration proof belongs in the final wave.</action>
  <acceptance_criteria>
    - `rg -n "getSessions|createSession|getSessionMessages|assistant session" src/services/storage/types.ts src/services/storage/electronStorage.ts src/services/storage/webStorage.ts`
    - `bun run vitest run test/services/assistantSessionPersistence.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantSessionPersistence.test.ts</automated>
  </verify>
  <done>The shared storage layer now speaks in canonical assistant sessions and session-scoped transcripts, which clears the persistence substrate for later app and channel work.</done>
</task>

</tasks>

<verification>
Run repository and storage tests, then confirm canonical session persistence is available through one shared Electron/web storage surface.
</verification>

<success_criteria>
Phase 2 has one durable session persistence path that app and future channel callers can both consume without reintroducing global chat history state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-routing-and-persistence/02-02-SUMMARY.md`
</output>
