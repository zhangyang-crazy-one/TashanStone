---
phase: 02-session-routing-and-persistence
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/services/assistant-runtime/sessionRouter.ts
  - src/services/assistant-runtime/sessionPolicy.ts
  - src/services/assistant-runtime/index.ts
  - test/services/assistantSessionRouter.test.ts
  - test/services/assistantActivationPolicy.test.ts
autonomous: true
requirements:
  - SESS-02
  - SESS-03
  - SESS-04
must_haves:
  truths:
    - "The shared runtime has one transport-neutral router that can resolve direct, group, and future channel routes into canonical assistant sessions."
    - "Activation policy is explicit and testable instead of being buried in UI or future channel adapter code."
    - "Reply-context metadata is normalized early enough that later channel phases can resume the right session and route safely."
  artifacts:
    - path: "src/services/assistant-runtime/sessionRouter.ts"
      provides: "Canonical route resolution and session-target mapping"
    - path: "src/services/assistant-runtime/sessionPolicy.ts"
      provides: "Activation and reply-context policy evaluation for inbound assistant routes"
    - path: "test/services/assistantSessionRouter.test.ts"
      provides: "Regression coverage for direct/group/channel route isolation and routing decisions"
  key_links:
    - from: "src/services/assistant-runtime/sessionRouter.ts"
      to: "src/services/assistant-runtime/sessionTypes.ts"
      via: "canonical routing resolves against the Phase 2 session contract surface"
      pattern: "Assistant(Session|Route)"
    - from: "src/services/assistant-runtime/sessionPolicy.ts"
      to: "src/services/assistant-runtime/sessionRouter.ts"
      via: "activation decisions consume resolved route metadata instead of transport-specific conditionals"
      pattern: "shouldInvoke|replyContext"
---

<objective>
Implement the routing and activation policy layer that decides which assistant session a message belongs to and whether it should invoke the runtime.

Purpose: Satisfy `SESS-02` and `SESS-03` before any real channel adapter ships, while also normalizing reply context for `SESS-04`.
Output: Session router, activation policy engine, reply-context normalization, and regression tests for isolated route behavior.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-routing-and-persistence/02-CONTEXT.md
@.planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
@.planning/phases/02-session-routing-and-persistence/02-01-PLAN.md
@src/services/assistant-runtime/types.ts
@src/services/assistant-runtime/index.ts
@src/app/hooks/useAIWorkflow.ts
@.planning/research/openclaw-initial-analysis.md

<interfaces>
From [src/services/assistant-runtime/types.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/assistant-runtime/types.ts):
```typescript
export interface AssistantRuntimeCaller {
  callerId: string;
  surface: AssistantCallerSurface;
  transport: AssistantCallerTransport;
  language: Language;
  capabilities: AssistantRuntimeCallerCapabilities;
  routeKey?: string;
}
```

From [src/services/assistant-runtime/types.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/assistant-runtime/types.ts):
```typescript
export interface AssistantRuntimeTransportMetadata {
  channel: AssistantTransportChannel;
  messageId?: string;
  correlationId?: string;
  replyTarget?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement transport-neutral session route resolution</name>
  <files>src/services/assistant-runtime/sessionRouter.ts, src/services/assistant-runtime/index.ts, test/services/assistantSessionRouter.test.ts</files>
  <read_first>
    - .planning/phases/02-session-routing-and-persistence/02-01-PLAN.md
    - src/services/assistant-runtime/types.ts
    - src/app/hooks/useAIWorkflow.ts
    - .planning/research/openclaw-initial-analysis.md
    - .planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Direct app routes resolve to a primary session model rather than an ad hoc request-local id.
    - Test 2: Group and channel-thread routes resolve to isolated canonical sessions via route metadata rather than collapsing into the primary session.
    - Test 3: Route resolution remains transport-neutral and reusable by future WhatsApp and QQ Channel adapters.
  </behavior>
  <action>Per D-03 and D-04, implement `sessionRouter.ts` under the runtime layer so direct, grouped, and future channel routes all resolve through one canonical route model. Reuse the Phase 2 session contracts instead of inventing transport-specific conditionals. Export the router through the runtime barrel so later app and channel callers can depend on it consistently.</action>
  <acceptance_criteria>
    - `rg -n "resolve.*Session|routeKey|group|channel" src/services/assistant-runtime/sessionRouter.ts src/services/assistant-runtime/index.ts`
    - `bun run vitest run test/services/assistantSessionRouter.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantSessionRouter.test.ts</automated>
  </verify>
  <done>The runtime now has one reusable route-resolution layer that enforces direct versus grouped session isolation before any channel adapter is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement activation and reply-context policy evaluation</name>
  <files>src/services/assistant-runtime/sessionPolicy.ts, test/services/assistantActivationPolicy.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/sessionTypes.ts
    - src/services/assistant-runtime/sessionRouter.ts
    - .planning/phases/02-session-routing-and-persistence/02-CONTEXT.md
    - .planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Activation decisions are explicit outputs from policy evaluation rather than implicit caller behavior.
    - Test 2: Reply-context metadata is normalized into a canonical structure that can be persisted and reused later.
    - Test 3: Policy evaluation remains generic enough for future channel adapters and does not hardcode WhatsApp or QQ specifics.
  </behavior>
  <action>Per D-04 and D-05, implement `sessionPolicy.ts` for activation and reply-context policy evaluation. The policy layer should decide invoke versus ignore based on canonical routing and caller metadata, and it should normalize reply-context inputs into a persisted-safe structure. Keep the work future-facing but Phase-2-sized: build the shared evaluation logic now, not concrete webhook adapters.</action>
  <acceptance_criteria>
    - `rg -n "shouldInvoke|replyContext|activation" src/services/assistant-runtime/sessionPolicy.ts`
    - `bun run vitest run test/services/assistantActivationPolicy.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantActivationPolicy.test.ts</automated>
  </verify>
  <done>The session layer now evaluates activation and reply-context behavior explicitly, which clears the routing policy obligations for later channel delivery phases.</done>
</task>

</tasks>

<verification>
Run the router and activation-policy tests, then confirm direct and grouped routes remain isolated and activation decisions are produced through shared policy code.
</verification>

<success_criteria>
Phase 2 has a reusable route-resolution and activation-policy layer that future channel adapters can consume without hardcoding transport-specific assistant logic into the app.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-routing-and-persistence/02-03-SUMMARY.md`
</output>
