---
phase: 02-session-routing-and-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types.ts
  - src/services/assistant-runtime/sessionTypes.ts
  - src/services/assistant-runtime/sessionRoutingConfig.ts
  - electron/database/schema.sql
  - electron/database/migrations.ts
  - test/services/assistantSessionContracts.test.ts
  - test/services/assistantSessionSchema.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
must_haves:
  truths:
    - "Maintainers can point to one canonical session and route contract instead of mixing runtime request ids, conversation ids, and context session ids."
    - "Session routing and activation metadata are defined as transport-neutral contracts before any channel adapter is implemented."
    - "The persistence schema has a clear home for canonical assistant sessions, route identity, and reply-context metadata."
  artifacts:
    - path: "src/services/assistant-runtime/sessionTypes.ts"
      provides: "Canonical assistant session, route, activation, and reply-context contracts"
    - path: "src/services/assistant-runtime/sessionRoutingConfig.ts"
      provides: "Typed defaults and config descriptors for route and activation policy inputs"
    - path: "test/services/assistantSessionContracts.test.ts"
      provides: "Regression coverage for stable session, route, and reply-context contract shapes"
  key_links:
    - from: "src/services/assistant-runtime/sessionTypes.ts"
      to: "src/services/assistant-runtime/types.ts"
      via: "canonical session contracts extend the Phase 1 runtime session surface instead of bypassing it"
      pattern: "Assistant(Session|Route|Reply)"
    - from: "electron/database/migrations.ts"
      to: "electron/database/schema.sql"
      via: "canonical session persistence and route metadata exist in both baseline schema and migrations"
      pattern: "assistant_sessions|reply_context"
---

<objective>
Define the Phase 2 session and routing substrate first so persistence, router behavior, and app wiring all implement the same identity model.

Purpose: Lock the canonical session and route contract per D-01 through D-07 before implementation splits across repositories and hooks.
Output: Session contracts, routing config defaults, schema/migration plan, and contract tests that downstream plans can build against.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-routing-and-persistence/02-CONTEXT.md
@.planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
@types.ts
@src/services/assistant-runtime/types.ts
@src/app/hooks/useAIWorkflow.ts
@electron/database/schema.sql
@electron/database/migrations.ts
@electron/database/repositories/chatRepository.ts

<interfaces>
From [src/services/assistant-runtime/types.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/assistant-runtime/types.ts):
```typescript
export interface AssistantSessionRef {
  sessionId: string;
  threadId?: string;
  scope: AssistantSessionScope;
  origin: AssistantSessionOrigin;
  parentSessionId?: string;
}
```

From [src/app/hooks/useAIWorkflow.ts](/home/zhangyangrui/my_programes/TashanStone/src/app/hooks/useAIWorkflow.ts):
```typescript
const sessionId = `app-chat-${requestId}`;
```

From [electron/database/repositories/chatRepository.ts](/home/zhangyangrui/my_programes/TashanStone/electron/database/repositories/chatRepository.ts):
```typescript
getAll(conversationId: string = 'default'): ChatMessage[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical assistant session, route, and reply-context contracts</name>
  <files>types.ts, src/services/assistant-runtime/sessionTypes.ts, src/services/assistant-runtime/sessionRoutingConfig.ts, test/services/assistantSessionContracts.test.ts</files>
  <read_first>
    - .planning/phases/02-session-routing-and-persistence/02-CONTEXT.md
    - .planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
    - src/services/assistant-runtime/types.ts
    - src/app/hooks/useAIWorkflow.ts
    - .planning/phases/01-assistant-runtime-foundation/01-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Canonical session contracts describe session identity, route identity, participant scope, lifecycle state, and reply-context metadata without importing UI-local state.
    - Test 2: Direct routes, group routes, and channel routes normalize through one transport-neutral contract surface.
    - Test 3: The new session and route types can be re-exported from `types.ts` or the runtime barrel without leaking UI hook state.
  </behavior>
  <action>Per D-01 through D-05, create the canonical Phase 2 contract layer in `src/services/assistant-runtime/sessionTypes.ts` plus any supporting typed defaults in `sessionRoutingConfig.ts`. Define serializable contracts for assistant sessions, session routes, route participants, activation policy inputs, activation decisions, and persisted reply-context metadata. Reconcile, but do not collapse blindly, the existing `AssistantSessionRef`, `conversation_id`, and checkpoint `session_id` concepts. Add focused regression tests that fail if later plans drift back toward request-local session ids or transport-specific routing assumptions.</action>
  <acceptance_criteria>
    - `rg -n "Assistant(Session|Route|Reply|Activation)" types.ts src/services/assistant-runtime/sessionTypes.ts src/services/assistant-runtime/sessionRoutingConfig.ts`
    - `bun run vitest run test/services/assistantSessionContracts.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantSessionContracts.test.ts</automated>
  </verify>
  <done>The repository exposes one canonical session and route contract surface, and regression tests guard it from slipping back into request-local or UI-local identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepare the canonical persistence schema and migrations</name>
  <files>electron/database/schema.sql, electron/database/migrations.ts, test/services/assistantSessionSchema.test.ts</files>
  <read_first>
    - electron/database/schema.sql
    - electron/database/migrations.ts
    - electron/database/repositories/chatRepository.ts
    - electron/database/repositories/contextRepository.ts
    - .planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The schema has a durable home for canonical session metadata and route identity instead of storing everything indirectly in `chat_messages`.
    - Test 2: Reply-context metadata and persisted routing information can be associated with canonical assistant sessions.
    - Test 3: Existing transcript, checkpoint, and compacted-session persistence remain compatible with the canonical session model.
  </behavior>
  <action>Per D-05 through D-08, extend the SQLite schema and migration history so canonical assistant sessions have a durable persistence home. Keep this Phase 2 foundation focused: introduce session metadata and route/reply-context fields or tables, and preserve compatibility with existing transcript plus checkpoint persistence. Do not implement repository behavior here; only make the schema and migration contract safe for downstream plans.</action>
  <acceptance_criteria>
    - `rg -n "assistant_sessions|route_key|reply_context|reply_target" electron/database/schema.sql electron/database/migrations.ts`
    - `bun run vitest run test/services/assistantSessionSchema.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantSessionSchema.test.ts</automated>
  </verify>
  <done>The database schema and migration history are ready for canonical session persistence, and later plans can implement repositories without inventing the storage shape on the fly.</done>
</task>

</tasks>

<verification>
Confirm the new session contracts stay transport-neutral and that the schema/migration layer has an explicit canonical home for assistant sessions plus reply-context metadata.
</verification>

<success_criteria>
Phase 2 starts with one coherent session model and one explicit persistence contract that every later routing and storage change can implement against.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-routing-and-persistence/02-01-SUMMARY.md`
</output>
