---
phase: 02-session-routing-and-persistence
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/app/hooks/useAssistantSessions.ts
  - src/app/hooks/useChatHistory.ts
  - src/app/hooks/useAIWorkflow.ts
  - test/services/inAppAssistantSessions.test.ts
  - test/services/inAppSessionRuntimeBridge.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-04
must_haves:
  truths:
    - "The in-app assistant uses stable session lifecycle state instead of a single global transcript and per-request session ids."
    - "Chat history loads and persists by active assistant session rather than one implicit `neon-chat-history` conversation."
    - "The app proves session isolation and resume behavior without requiring the full later-phase assistant parity UI."
  artifacts:
    - path: "src/app/hooks/useAssistantSessions.ts"
      provides: "Minimal app-facing session lifecycle hook for create/resume/switch behavior"
    - path: "src/app/hooks/useChatHistory.ts"
      provides: "Session-aware transcript loading and persistence in the app layer"
    - path: "test/services/inAppSessionRuntimeBridge.test.ts"
      provides: "Regression coverage for stable runtime session reuse and session-scoped chat history"
  key_links:
    - from: "src/app/hooks/useAssistantSessions.ts"
      to: "src/services/storage/types.ts"
      via: "app session lifecycle state uses the shared session-oriented storage surface"
      pattern: "getSessions|createSession|setActive"
    - from: "src/app/hooks/useAIWorkflow.ts"
      to: "src/services/assistant-runtime/sessionRouter.ts"
      via: "the app runtime bridge uses canonical session routing instead of request-local ids"
      pattern: "sessionId|routeKey"
---

<objective>
Close Phase 2 by wiring the app onto the canonical session model so session isolation and durable resume behavior are proven in a real caller.

Purpose: Prove `SESS-01` and `SESS-04` end to end while keeping full in-app assistant parity work scoped to later phases.
Output: Minimal app session lifecycle hook, session-aware chat history, runtime session reuse in the app caller, and regression coverage for isolated conversations.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-routing-and-persistence/02-CONTEXT.md
@.planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
@.planning/phases/02-session-routing-and-persistence/02-02-PLAN.md
@.planning/phases/02-session-routing-and-persistence/02-03-PLAN.md
@src/app/hooks/useChatHistory.ts
@src/app/hooks/useAIWorkflow.ts
@src/services/storage/types.ts
@src/services/storage/storageService.ts
@src/services/assistant-runtime/sessionRouter.ts

<interfaces>
From [src/app/hooks/useChatHistory.ts](/home/zhangyangrui/my_programes/TashanStone/src/app/hooks/useChatHistory.ts):
```typescript
const saved = localStorage.getItem('neon-chat-history');
```

From [src/app/hooks/useAIWorkflow.ts](/home/zhangyangrui/my_programes/TashanStone/src/app/hooks/useAIWorkflow.ts):
```typescript
const requestId = generateId();
const sessionId = `app-chat-${requestId}`;
```

From [src/services/storage/types.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/storage/types.ts):
```typescript
export interface StorageService {
  getChatMessages(conversationId?: string): Promise<ChatMessage[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a minimal app-facing assistant session lifecycle hook</name>
  <files>src/app/hooks/useAssistantSessions.ts, src/app/hooks/useChatHistory.ts, test/services/inAppAssistantSessions.test.ts</files>
  <read_first>
    - src/app/hooks/useChatHistory.ts
    - src/services/storage/types.ts
    - src/services/storage/storageService.ts
    - .planning/phases/02-session-routing-and-persistence/02-CONTEXT.md
    - .planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The app can create, resume, and switch assistant sessions without collapsing all chat history into one transcript.
    - Test 2: Chat history is loaded and saved by active session id rather than one implicit browser key.
    - Test 3: The app session hook remains a minimal lifecycle seam, not a full Phase 4 UI implementation.
  </behavior>
  <action>Per D-07 through D-09, add a minimal `useAssistantSessions.ts` hook and make `useChatHistory.ts` session-aware. The goal is to prove isolated app-side conversations and durable resume behavior through hooks and storage, not to ship the full later-phase assistant UI. Remove the single-global-history assumption from the app layer while preserving current notebook workflows.</action>
  <acceptance_criteria>
    - `rg -n "useAssistantSessions|activeSessionId|getSessionMessages" src/app/hooks/useAssistantSessions.ts src/app/hooks/useChatHistory.ts`
    - `bun run vitest run test/services/inAppAssistantSessions.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantSessions.test.ts</automated>
  </verify>
  <done>The app has a minimal but real session lifecycle seam, and chat history is scoped by active assistant session instead of one browser-global transcript.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the in-app runtime bridge to stable assistant sessions</name>
  <files>src/app/hooks/useAIWorkflow.ts, test/services/inAppSessionRuntimeBridge.test.ts</files>
  <read_first>
    - src/app/hooks/useAIWorkflow.ts
    - src/services/assistant-runtime/sessionRouter.ts
    - src/services/assistant-runtime/sessionPolicy.ts
    - src/services/storage/types.ts
    - .planning/phases/02-session-routing-and-persistence/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Repeated messages in one app conversation reuse a stable canonical session instead of generating a new session per request.
    - Test 2: Session-aware chat persistence and runtime routing remain aligned, so app transcript state and runtime session refs do not drift apart.
    - Test 3: The in-app bridge preserves existing notebook-native runtime behavior while moving onto the canonical session model.
  </behavior>
  <action>Per D-01, D-02, and D-10, update `useAIWorkflow.ts` so the app caller uses the canonical session and routing layer instead of request-local session ids. Preserve the runtime-backed architecture delivered in Phase 1, but align runtime requests, chat history, and session lifecycle state around the same active session identity. Keep the change narrowly scoped to the bridge and regression coverage rather than a broader UI rework.</action>
  <acceptance_criteria>
    - `rg -n "activeSessionId|sessionId|routeKey" src/app/hooks/useAIWorkflow.ts`
    - `bun run vitest run test/services/inAppSessionRuntimeBridge.test.ts`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppSessionRuntimeBridge.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>The in-app assistant now runs on stable assistant sessions with session-scoped history and no longer relies on disposable per-request runtime identities.</done>
</task>

</tasks>

<verification>
Run the app session lifecycle and runtime-bridge tests, then confirm the app now uses stable session ids with session-scoped transcript persistence.
</verification>

<success_criteria>
Phase 2 ends with one canonical session model proven in real app wiring, while full in-app parity and channel delivery remain cleanly deferred to later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-routing-and-persistence/02-04-SUMMARY.md`
</output>
