---
phase: 01-assistant-runtime-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/app/hooks/useAIWorkflow.ts
  - src/app/hooks/useAppConfig.ts
  - components/AISettingsModal.tsx
  - electron/database/repositories/configRepository.ts
  - src/services/storage/electronStorage.ts
  - test/services/inAppAssistantRuntimeAdapter.test.ts
  - test/services/assistantSettingsPersistence.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-04
must_haves:
  truths:
    - "The current in-app assistant flow calls the shared runtime instead of owning assistant orchestration inside `useAIWorkflow`."
    - "Phase 1 config persistence can store the runtime/schema fields needed by the wireframed settings surfaces without forcing full UI delivery."
    - "The extraction preserves existing notebook workflows while proving the runtime can be consumed by a real app caller."
  artifacts:
    - path: "src/app/hooks/useAIWorkflow.ts"
      provides: "In-app caller adapter around the shared runtime"
    - path: "electron/database/repositories/configRepository.ts"
      provides: "Persistence shape that can hold Phase 1 runtime/config schema"
    - path: "test/services/inAppAssistantRuntimeAdapter.test.ts"
      provides: "Regression coverage for the proof caller migration"
  key_links:
    - from: "src/app/hooks/useAIWorkflow.ts"
      to: "src/services/assistant-runtime/createAssistantRuntime.ts"
      via: "hook becomes caller adapter rather than orchestration owner"
      pattern: "createAssistantRuntime|execute"
    - from: "src/app/hooks/useAppConfig.ts"
      to: "electron/database/repositories/configRepository.ts"
      via: "descriptor-backed config persistence replaces UI-local drift"
      pattern: "getAIConfig|setAIConfig"
---

<objective>
Prove the extraction end to end by moving one real in-app caller onto the runtime and wiring the minimum config persistence/shell substrate needed for the wireframed settings model.

Purpose: Close the Phase 1 loop without swallowing later UI or channel phases, per D-09, D-10, and D-12.
Output: A runtime-backed in-app caller, expanded config persistence, and a minimal descriptor-aware settings shell path.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
@src/app/hooks/useAIWorkflow.ts
@src/app/hooks/useAppConfig.ts
@components/AISettingsModal.tsx
@electron/database/repositories/configRepository.ts
@src/services/storage/electronStorage.ts
@src/hooks/useStorage.ts

<interfaces>
From [src/app/hooks/useAIWorkflow.ts](/home/zhangyangrui/my_programes/TashanStone/src/app/hooks/useAIWorkflow.ts):
```typescript
interface UseAIWorkflowResult {
  handleChatMessage: (text: string) => Promise<void>;
  handleStopStreaming: () => void;
  handleCompactChat: () => Promise<void>;
}
```

From [src/app/hooks/useAppConfig.ts](/home/zhangyangrui/my_programes/TashanStone/src/app/hooks/useAppConfig.ts):
```typescript
interface UseAppConfigResult {
  aiConfig: AIConfig;
  setAiConfig: Dispatch<SetStateAction<AIConfig>>;
  handleSettingsSave: (config: AIConfig) => Promise<void>;
}
```

From [src/services/storage/electronStorage.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/storage/electronStorage.ts):
```typescript
async getAIConfig(): Promise<AIConfig> {
  return await window.electronAPI.db.config.get();
}

async setAIConfig(config: AIConfig): Promise<AIConfig> {
  return await window.electronAPI.db.config.set(config);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the current in-app assistant flow to a runtime caller adapter</name>
  <files>src/app/hooks/useAIWorkflow.ts, test/services/inAppAssistantRuntimeAdapter.test.ts</files>
  <behavior>
    - Test 1: Sending a chat message from the existing in-app flow invokes the shared runtime rather than building execution inside the hook.
    - Test 2: Streaming updates, tool status, and stop/cancel behavior still reach the UI through adapter logic.
    - Test 3: Notebook file mutations and knowledge search calls remain functional because the hook now translates runtime events instead of re-owning orchestration.
  </behavior>
  <action>Refactor `useAIWorkflow.ts` into a real caller adapter around the runtime from Plan 02, per D-01 and D-10. Keep the existing chat-panel behavior intact, but move execution ownership out of the hook. The hook may translate runtime events into `setChatMessages`, `setAiState`, and file mutations, yet it must stop being the place where request normalization, provider selection, and tool/event orchestration live. Preserve existing notebook workflows while making the dependency direction explicit: UI calls runtime, runtime does not call UI.</action>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantRuntimeAdapter.test.ts</automated>
  </verify>
  <done>The in-app assistant path is a caller of the runtime, not the owner of assistant orchestration, and core notebook behaviors still work through the adapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire minimal Phase 1 config persistence and descriptor-aware settings shell</name>
  <files>src/app/hooks/useAppConfig.ts, components/AISettingsModal.tsx, electron/database/repositories/configRepository.ts, src/services/storage/electronStorage.ts, test/services/assistantSettingsPersistence.test.ts</files>
  <behavior>
    - Test 1: Saving Phase 1 runtime/config fields goes through the storage/database path rather than only UI-local localStorage.
    - Test 2: The minimal settings shell can read the descriptor-backed section metadata without implementing every wireframed page.
    - Test 3: New persisted fields cover the runtime/config substrate needed by the designed AI/operator and notebook/app surfaces, while deferred sections remain stubs or metadata-only.
  </behavior>
  <action>Introduce the smallest persistence and shell integration that makes the Phase 1 schema real, per D-04, D-05, D-06, D-09, D-11, and D-12. Expand `configRepository` and the Electron storage path so the runtime/config fields introduced in earlier plans can round-trip through the app’s storage layer instead of drifting in `useAppConfig` localStorage only. Update `useAppConfig` to use that storage-backed path. In `AISettingsModal.tsx`, add only the descriptor-aware shell or section registry integration needed to prove the wireframe-aligned schema is consumable; do not build every page from the Pencil file and do not expand into channel adapters, full notebook settings parity, or broad visual rework.</action>
  <verify>
    <automated>bun run vitest run test/services/assistantSettingsPersistence.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>The app has one real runtime-backed caller and one real persistence path for the new schema, while full settings-page delivery remains deferred to later phases.</done>
</task>

</tasks>

<verification>
Launch the targeted tests and confirm the migrated hook is only an adapter, while the config path now persists Phase 1 runtime/schema fields through the shared storage stack.
</verification>

<success_criteria>
Phase 1 ends with a runtime-backed proof caller and a minimal, wireframe-aligned config substrate, without swallowing later UI or channel work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-assistant-runtime-foundation/01-assistant-runtime-foundation-03-SUMMARY.md`
</output>
