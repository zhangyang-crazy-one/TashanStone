---
phase: 01-assistant-runtime-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/hooks/useAppConfig.ts
  - components/AISettingsModal.tsx
  - electron/database/repositories/configRepository.ts
  - src/services/storage/electronStorage.ts
  - test/services/assistantSettingsPersistence.test.ts
  - test/components/AISettingsModal.runtimeShell.test.tsx
autonomous: true
requirements:
  - CORE-01
  - CORE-04
must_haves:
  truths:
    - "The runtime and settings schema can round-trip through the shared app config path instead of living in UI-only state."
    - "The minimal settings shell reads descriptor-backed surfaces and sections rather than hardcoded tab ids."
    - "Operator settings visibly preserve Plugins and Skills as separate sections in the shell metadata."
  artifacts:
    - path: "electron/database/repositories/configRepository.ts"
      provides: "Persistence shape for Phase 01 runtime and assistant settings data"
    - path: "src/services/storage/electronStorage.ts"
      provides: "Shared storage normalization for persisted assistant config"
    - path: "components/AISettingsModal.tsx"
      provides: "Descriptor-aware settings shell that can surface operator and notebook section catalogs"
  key_links:
    - from: "src/app/hooks/useAppConfig.ts"
      to: "src/services/storage/electronStorage.ts"
      via: "app config reads and saves through the shared storage service"
      pattern: "getAIConfig|setAIConfig"
    - from: "components/AISettingsModal.tsx"
      to: "src/services/assistant-runtime/settingsCatalog.ts"
      via: "settings shell resolves operator and notebook sections from the descriptor catalog"
      pattern: "resolveAssistantSettingsSurface|ASSISTANT_SETTINGS_SURFACE_ORDER"
---

<objective>
Make the Phase 01 settings schema real in storage and UI shell terms without expanding into full settings-page implementation.

Purpose: Persist the new runtime/config substrate and prove the TUI-aligned schema, including explicit Plugins and Skills sections, is consumable by the existing app per D-04 through D-11.
Output: Shared config persistence, descriptor-aware settings shell wiring, and regression tests for schema round-tripping.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
@types.ts
@src/app/hooks/useAppConfig.ts
@components/AISettingsModal.tsx
@electron/database/repositories/configRepository.ts
@src/services/storage/electronStorage.ts
@src/services/assistant-runtime/settingsCatalog.ts

<interfaces>
From [electron/database/repositories/configRepository.ts](/home/zhangyangrui/my_programes/TashanStone/electron/database/repositories/configRepository.ts):
```typescript
export class ConfigRepository {
  getAIConfig(): AIConfig { ... }
  setAIConfig(config: AIConfig): AIConfig { ... }
}
```

From [src/services/storage/electronStorage.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/storage/electronStorage.ts):
```typescript
async getAIConfig(): Promise<AIConfig> {
  const config = await window.electronAPI.db.config.get();
  return normalizeAIConfig(config);
}

async setAIConfig(config: AIConfig): Promise<AIConfig> {
  const savedConfig = await window.electronAPI.db.config.set(config);
  return normalizeAIConfig(savedConfig);
}
```

From [components/AISettingsModal.tsx](/home/zhangyangrui/my_programes/TashanStone/components/AISettingsModal.tsx):
```typescript
const settingsShellSurfaces = useMemo(
  () => ASSISTANT_SETTINGS_SURFACE_ORDER.map(surfaceId => resolveAssistantSettingsSurface(currentUiLang, surfaceId)),
  [currentUiLang],
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist the Phase 01 assistant config through the shared storage path</name>
  <files>src/app/hooks/useAppConfig.ts, electron/database/repositories/configRepository.ts, src/services/storage/electronStorage.ts, test/services/assistantSettingsPersistence.test.ts</files>
  <read_first>
    - types.ts
    - src/app/hooks/useAppConfig.ts
    - electron/database/repositories/configRepository.ts
    - src/services/storage/electronStorage.ts
    - .planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Saving the Phase 01 assistant config flows through the repository/storage path rather than only UI-local state.
    - Test 2: Stored config values normalize and merge correctly with defaults, including assistant surface/section selection.
    - Test 3: The persistence layer can store the descriptor-backed runtime settings substrate without requiring full settings-page implementation.
  </behavior>
  <action>Per D-04, D-05, D-09, and D-11, wire `useAppConfig` to the shared storage path and keep `configRepository` plus `electronStorage` responsible for merging and round-tripping the extended `AIConfig` shape. Use the existing settings table / config repository seam instead of inventing a second persistence store. Keep the change Phase-01-sized: this is config substrate work, not a full settings UI buildout.</action>
  <acceptance_criteria>
    - `rg -n "assistantSettings|getAIConfig|setAIConfig|normalizeAIConfig" src/app/hooks/useAppConfig.ts electron/database/repositories/configRepository.ts src/services/storage/electronStorage.ts`
    - `bun run vitest run test/services/assistantSettingsPersistence.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantSettingsPersistence.test.ts</automated>
  </verify>
  <done>The Phase 01 assistant config persists through the shared repository/storage path and no longer depends on UI-only state to stay stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the descriptor-aware settings shell with separate Plugins and Skills sections</name>
  <files>components/AISettingsModal.tsx, test/components/AISettingsModal.runtimeShell.test.tsx</files>
  <read_first>
    - components/AISettingsModal.tsx
    - src/services/assistant-runtime/settingsCatalog.ts
    - src/services/assistant-runtime/defaults.ts
    - utils/translations.ts
    - .planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The shell reads surface and section metadata from the descriptor catalog instead of relying on a hardcoded settings IA.
    - Test 2: Operator settings show `plugins` and `skills` as distinct shell sections, not merged into `tools` or `agents`.
    - Test 3: The shell stays minimal and metadata-driven; it does not attempt to implement every designed settings page in Phase 01.
  </behavior>
  <action>Per D-04, D-05, D-06, D-07, D-09, and D-11, update `AISettingsModal.tsx` so the Phase 01 shell consumes the descriptor catalog and reflects the locked TUI settings split. Treat the user correction as non-negotiable: Plugins and Skills must appear as separate operator sections in the shell metadata path. Do not build the full operator/notebook settings pages here, per D-12. This task is only about proving the schema is renderable and selectable through the current settings surface.</action>
  <acceptance_criteria>
    - `rg -n "resolveAssistantSettingsSurface|ASSISTANT_SETTINGS_SURFACE_ORDER|plugins|skills" components/AISettingsModal.tsx src/services/assistant-runtime/settingsCatalog.ts`
    - `bun run vitest run test/components/AISettingsModal.runtimeShell.test.tsx`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/components/AISettingsModal.runtimeShell.test.tsx && bun run tsc --noEmit</automated>
  </verify>
  <done>The existing settings modal can consume the descriptor catalog, preserve the corrected operator section structure, and remain a minimal Phase 01 shell instead of ballooning into full page delivery.</done>
</task>

</tasks>

<verification>
Run the persistence and shell tests, then confirm the config path is storage-backed and the shell resolves explicit Plugins and Skills sections from descriptor metadata.
</verification>

<success_criteria>
Phase 01 has one shared persistence path for assistant config and one descriptor-aware settings shell that preserves the locked TUI information architecture without over-scoping into full UI delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/01-assistant-runtime-foundation/01-03-SUMMARY.md`
</output>
