---
phase: 01-assistant-runtime-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/services/assistant-runtime/contextAdapters.ts
  - src/services/assistant-runtime/contextAssembler.ts
  - src/services/assistant-runtime/createAssistantRuntime.ts
  - src/services/assistant-runtime/index.ts
  - src/app/hooks/useAIWorkflow.ts
  - test/services/assistantRuntime.context.test.ts
  - test/services/inAppAssistantRuntimeAdapter.test.ts
autonomous: true
requirements:
  - CORE-03
  - CORE-04
gap_closure: true
must_haves:
  truths:
    - "A notebook-backed runtime call assembles notebook notes, workspace state, and knowledge context in shipped production wiring instead of only in tests."
    - "The in-app caller creates the assistant runtime with a non-empty adapter-backed context assembler instead of the default empty assembler."
    - "The same production adapter factory can be reused by later non-UI callers without importing React state into the runtime layer."
  artifacts:
    - path: "src/services/assistant-runtime/contextAdapters.ts"
      provides: "Production assistant context adapter builders and the shared notebook context-assembler factory"
    - path: "src/app/hooks/useAIWorkflow.ts"
      provides: "In-app runtime construction wired to the production notebook/workspace/knowledge adapters"
    - path: "test/services/inAppAssistantRuntimeAdapter.test.ts"
      provides: "Regression coverage that the in-app caller no longer relies on an empty default assembler"
  key_links:
    - from: "src/services/assistant-runtime/contextAdapters.ts"
      to: "src/services/assistant-runtime/contextAssembler.ts"
      via: "shared factory that registers notebook/workspace/knowledge adapters before runtime execution"
      pattern: "createNotebookContextAssembler"
    - from: "src/app/hooks/useAIWorkflow.ts"
      to: "src/services/assistant-runtime/contextAdapters.ts"
      via: "runtimeRef creation with file-backed and knowledge-backed adapter dependencies"
      pattern: "createNotebookContextAssembler"
    - from: "src/services/assistant-runtime/contextAdapters.ts"
      to: "vectorStore.searchWithResults"
      via: "knowledge adapter dependency callback that turns search results into runtime context sections"
      pattern: "searchWithResults"
---

<objective>
Close the verified CORE-03 production gap by registering real notebook/workspace/knowledge adapters in shipped runtime wiring instead of relying on test-only adapters or the empty assembler default.

Purpose: Finish D-03 and preserve D-10 by keeping notebook-native context assembly inside the runtime substrate while the in-app hook remains only a caller adapter.
Output: Production context adapter builders, a reusable adapter-backed context-assembler factory, and runtime wiring/tests proving the in-app caller no longer executes with empty adapters.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
@.planning/phases/01-assistant-runtime-foundation/01-VERIFICATION.md
@.planning/phases/01-assistant-runtime-foundation/01-assistant-runtime-foundation-02-SUMMARY.md
@.planning/phases/01-assistant-runtime-foundation/01-assistant-runtime-foundation-03-SUMMARY.md
@src/services/assistant-runtime/createAssistantRuntime.ts
@src/services/assistant-runtime/contextAssembler.ts
@src/app/hooks/useAIWorkflow.ts
@test/services/assistantRuntime.context.test.ts
@test/services/inAppAssistantRuntimeAdapter.test.ts

<interfaces>
From `src/services/assistant-runtime/contextAssembler.ts`:
```typescript
export interface ContextAssemblerDependencies {
  adapters?: AssistantContextAdapter[];
  contextInjector?: ContextInjector;
  now?: () => number;
}

export function createContextAssembler(
  dependencies: ContextAssemblerDependencies = {},
): AssistantRuntimeContextAssembler;
```

From `src/services/assistant-runtime/createAssistantRuntime.ts`:
```typescript
export interface AssistantRuntimeDependencies {
  now?: () => number;
  contextAssembler?: AssistantRuntimeContextAssembler;
  providerExecution?: AssistantProviderExecution;
}

export function createAssistantRuntime(
  dependencies: AssistantRuntimeDependencies = {},
): AssistantRuntime;
```

From `src/services/assistant-runtime/types.ts`:
```typescript
export interface AssistantContextAdapter {
  adapterId: string;
  kind: AssistantContextAdapterKind;
  assemble: (
    input: AssistantNotebookContextInput,
    request?: Pick<AssistantRuntimeRequest, 'session' | 'caller' | 'input'>,
  ) => Promise<AssistantContextPayload> | AssistantContextPayload;
}
```

From `src/app/hooks/useAIWorkflow.ts`:
```typescript
const runtimeRef = useRef(createAssistantRuntime());

const runtimeRequest = {
  notebook: {
    notebookId: 'in-app-notebook',
    activeFileId: filesRef.current[0]?.id,
    selectedFileIds: filesRef.current.map(file => file.id),
    knowledgeQuery: text,
  },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the production notebook/workspace/knowledge adapter factory</name>
  <files>src/services/assistant-runtime/contextAdapters.ts, src/services/assistant-runtime/contextAssembler.ts, src/services/assistant-runtime/createAssistantRuntime.ts, src/services/assistant-runtime/index.ts, test/services/assistantRuntime.context.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/types.ts
    - src/services/assistant-runtime/contextAssembler.ts
    - src/services/assistant-runtime/createAssistantRuntime.ts
    - test/services/assistantRuntime.context.test.ts
    - .planning/phases/01-assistant-runtime-foundation/01-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Production code exposes real notebook-notes, workspace-state, and knowledge-context adapter builders instead of only test-local adapters.
    - Test 2: A shared `createNotebookContextAssembler(...)` factory registers those adapters through `createContextAssembler(...)` and assembles all three payload classes in one request.
    - Test 3: The adapter factory accepts dependency callbacks or plain data providers so later CLI/channel callers can reuse it without importing React hooks.
  </behavior>
  <action>Create `src/services/assistant-runtime/contextAdapters.ts` and move the production registration seam there per D-03. Implement concrete builders for notebook notes, workspace state, and knowledge context that consume dependency callbacks rather than React state directly. Use current notebook ids, selected file ids, file content, and existing knowledge-search results to build real `AssistantContextPayload` sections; do not reintroduce test-only hardcoded strings as the production implementation. Export a shared `createNotebookContextAssembler(...)` helper that wraps `createContextAssembler({ adapters })`, and re-export it from `src/services/assistant-runtime/index.ts`. Keep `createAssistantRuntime()` test-friendly, but remove the assumption that production callers should use the empty default assembler. Do not expand into channel or multimodal adapters; this plan only closes the CORE-03 gap identified in verification.</action>
  <acceptance_criteria>
    - `rg -n "createNotebookContextAssembler|createNotebookNotesContextAdapter|createWorkspaceStateContextAdapter|createKnowledgeContextAdapter" src/services/assistant-runtime`
    - `bun run vitest run test/services/assistantRuntime.context.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantRuntime.context.test.ts</automated>
  </verify>
  <done>The runtime layer has a shipped, reusable adapter-registration path for notebook notes, workspace state, and knowledge context, and tests prove it no longer depends on test-only adapter definitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the in-app caller to the production adapter-backed assembler</name>
  <files>src/app/hooks/useAIWorkflow.ts, test/services/inAppAssistantRuntimeAdapter.test.ts</files>
  <read_first>
    - src/app/hooks/useAIWorkflow.ts
    - src/services/assistant-runtime/contextAdapters.ts
    - test/services/inAppAssistantRuntimeAdapter.test.ts
    - .planning/phases/01-assistant-runtime-foundation/01-assistant-runtime-foundation-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `useAIWorkflow` creates its runtime with `contextAssembler: createNotebookContextAssembler(...)` instead of `createAssistantRuntime()` with empty defaults.
    - Test 2: The in-app adapter still translates runtime events into chat/tool/file UI state while notebook notes, workspace state, and knowledge data are sourced through the new production adapters.
    - Test 3: Regression coverage fails if the hook stops passing real file/knowledge dependencies into the runtime factory.
  </behavior>
  <action>Update `useAIWorkflow.ts` so `runtimeRef` is created with the shared adapter-backed assembler from Task 1, per D-03 and D-10. Use existing hook inputs such as `filesRef`, `vectorStore`, `handleIndexKnowledgeBase`, and current file metadata to supply notebook notes, workspace state, and knowledge-search dependencies without moving execution ownership back into the hook. Keep the hook as a caller adapter: it may translate runtime events into UI state, but it must not re-own context assembly logic. Extend `test/services/inAppAssistantRuntimeAdapter.test.ts` so it proves `createAssistantRuntime` receives a `contextAssembler` and that notebook/knowledge dependencies are threaded into the runtime construction path. Do not reopen config persistence, settings shell work, or provider orchestration; those passed verification already.</action>
  <acceptance_criteria>
    - `rg -n "createNotebookContextAssembler|contextAssembler" src/app/hooks/useAIWorkflow.ts test/services/inAppAssistantRuntimeAdapter.test.ts`
    - `bun run vitest run test/services/inAppAssistantRuntimeAdapter.test.ts test/services/assistantRuntime.context.test.ts`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantRuntimeAdapter.test.ts test/services/assistantRuntime.context.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>The shipped in-app caller no longer instantiates the runtime with empty adapters, and the regression suite protects the adapter-backed context path for both current and future callers.</done>
</task>

</tasks>

<verification>
Run the targeted runtime-context and in-app adapter tests, then confirm the repository contains a production adapter factory and that `useAIWorkflow` references it directly instead of calling `createAssistantRuntime()` with defaults.
</verification>

<success_criteria>
Phase 01 has a single focused gap-closure plan that resolves the verified CORE-03 production wiring failure without reopening already-passed provider, settings, or persistence work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-assistant-runtime-foundation/01-assistant-runtime-foundation-04-SUMMARY.md`
</output>
