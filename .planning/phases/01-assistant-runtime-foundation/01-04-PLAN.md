---
phase: 01-assistant-runtime-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/services/assistant-runtime/contextAdapters.ts
  - src/services/assistant-runtime/contextAssembler.ts
  - src/services/assistant-runtime/index.ts
  - src/app/hooks/useAIWorkflow.ts
  - test/services/assistantRuntime.context.test.ts
  - test/services/inAppAssistantRuntimeAdapter.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "A notebook-backed request uses shipped notebook, workspace, and knowledge adapters instead of test-only context wiring."
    - "The in-app assistant flow calls the shared runtime as a caller adapter instead of owning execution and context assembly."
    - "The production adapter factory can be reused by future callers without importing React state into the runtime layer."
  artifacts:
    - path: "src/services/assistant-runtime/contextAdapters.ts"
      provides: "Production notebook/workspace/knowledge adapter builders and a shared notebook assembler factory"
    - path: "src/app/hooks/useAIWorkflow.ts"
      provides: "In-app caller adapter that constructs and consumes the runtime"
    - path: "test/services/inAppAssistantRuntimeAdapter.test.ts"
      provides: "Regression coverage for the runtime-backed in-app caller"
  key_links:
    - from: "src/services/assistant-runtime/contextAdapters.ts"
      to: "src/services/assistant-runtime/contextAssembler.ts"
      via: "production adapter builders are registered through a shared notebook context assembler"
      pattern: "createNotebookContextAssembler"
    - from: "src/app/hooks/useAIWorkflow.ts"
      to: "src/services/assistant-runtime/createAssistantRuntime.ts"
      via: "the hook becomes a runtime caller adapter rather than orchestration owner"
      pattern: "createAssistantRuntime|createNotebookContextAssembler"
---

<objective>
Close Phase 01 with real production wiring: shipped notebook/workspace/knowledge adapters and one in-app caller migrated onto the shared runtime.

Purpose: Prove the runtime extraction end to end while preserving existing notebook workflows per D-03 and D-10.
Output: Production context adapter builders, a reusable notebook assembler factory, and a runtime-backed in-app caller with regression coverage.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
@src/services/assistant-runtime/contextAssembler.ts
@src/services/assistant-runtime/createAssistantRuntime.ts
@src/app/hooks/useAIWorkflow.ts
@src/services/assistant-runtime/types.ts

<interfaces>
From [src/services/assistant-runtime/contextAssembler.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/assistant-runtime/contextAssembler.ts):
```typescript
export interface ContextAssemblerDependencies {
  adapters?: AssistantContextAdapter[];
  contextInjector?: ContextInjector;
  now?: () => number;
}

export function createContextAssembler(
  dependencies: ContextAssemblerDependencies = {},
): AssistantRuntimeContextAssembler;
```

From [src/app/hooks/useAIWorkflow.ts](/home/zhangyangrui/my_programes/TashanStone/src/app/hooks/useAIWorkflow.ts):
```typescript
const runtimeRef = useRef<AssistantRuntime | null>(null);
if (!runtimeRef.current) {
  const contextAssembler = createNotebookContextAssembler({ ... });
  runtimeRef.current = createAssistantRuntime({ contextAssembler });
}
```

From [src/services/assistant-runtime/types.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/assistant-runtime/types.ts):
```typescript
export interface AssistantContextAdapter {
  adapterId: string;
  kind: AssistantContextAdapterKind;
  assemble: (
    input: AssistantNotebookContextInput,
    request?: Pick<AssistantRuntimeRequest, 'session' | 'caller' | 'input'>,
  ) => Promise<AssistantContextPayload> | AssistantContextPayload;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship the production notebook/workspace/knowledge adapter factory</name>
  <files>src/services/assistant-runtime/contextAdapters.ts, src/services/assistant-runtime/contextAssembler.ts, src/services/assistant-runtime/index.ts, test/services/assistantRuntime.context.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/types.ts
    - src/services/assistant-runtime/contextAssembler.ts
    - src/app/hooks/useAIWorkflow.ts
    - test/services/assistantRuntime.context.test.ts
    - .planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Production code exports notebook-notes, workspace-state, and knowledge adapter builders instead of only test-local adapter stubs.
    - Test 2: A shared `createNotebookContextAssembler(...)` registers those adapters through the runtime context assembler.
    - Test 3: The adapter builders accept dependency callbacks or plain providers so future callers can reuse them without importing React hooks.
  </behavior>
  <action>Per D-03 and D-10, create the production adapter registration seam in `contextAdapters.ts` and export it from the runtime index. Build notebook-notes, workspace-state, and knowledge adapters against dependency callbacks and existing file/vector-search data, not hardcoded test strings. Keep the factory reusable by future non-UI callers. Do not add channel adapters, multimodal logic, or unrelated settings work here, per D-12.</action>
  <acceptance_criteria>
    - `rg -n "createNotebookContextAssembler|createNotebookNotesContextAdapter|createWorkspaceStateContextAdapter|createKnowledgeContextAdapter" src/services/assistant-runtime`
    - `bun run vitest run test/services/assistantRuntime.context.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantRuntime.context.test.ts</automated>
  </verify>
  <done>The runtime layer ships reusable notebook/workspace/knowledge adapter builders and a shared assembler factory that production callers can consume directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor the in-app AI flow into a runtime caller adapter</name>
  <files>src/app/hooks/useAIWorkflow.ts, test/services/inAppAssistantRuntimeAdapter.test.ts</files>
  <read_first>
    - src/app/hooks/useAIWorkflow.ts
    - src/services/assistant-runtime/createAssistantRuntime.ts
    - src/services/assistant-runtime/contextAdapters.ts
    - test/services/inAppAssistantRuntimeAdapter.test.ts
    - .planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Sending a chat message from the current in-app flow invokes the shared runtime instead of hook-owned execution logic.
    - Test 2: Streaming updates, tool status, stop handling, and notebook side effects still reach the UI through adapter logic.
    - Test 3: Regression coverage fails if `useAIWorkflow` stops passing real notebook/workspace/knowledge dependencies into the runtime construction path.
  </behavior>
  <action>Per D-01, D-03, and D-10, refactor `useAIWorkflow.ts` so it becomes a caller adapter around `createAssistantRuntime({ contextAssembler: createNotebookContextAssembler(...) })`. Preserve current notebook behavior by translating runtime events into chat state, tool state, and file mutations, but stop owning request normalization, provider orchestration, or context assembly inside the hook. Keep the dependency direction explicit: UI calls runtime; runtime does not call UI.</action>
  <acceptance_criteria>
    - `rg -n "createAssistantRuntime|createNotebookContextAssembler|runtimeRef" src/app/hooks/useAIWorkflow.ts`
    - `bun run vitest run test/services/inAppAssistantRuntimeAdapter.test.ts test/services/assistantRuntime.context.test.ts`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/inAppAssistantRuntimeAdapter.test.ts test/services/assistantRuntime.context.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>The in-app AI flow is a runtime caller adapter with production context wiring, and the regression suite protects that ownership boundary.</done>
</task>

</tasks>

<verification>
Run the in-app adapter and runtime-context tests, then confirm the shipped code constructs the runtime with production notebook/workspace/knowledge adapters instead of default empty wiring.
</verification>

<success_criteria>
Phase 01 finishes with a runtime-backed in-app caller, shipped production context adapters, and no remaining dependency on the old hook path for core assistant orchestration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-assistant-runtime-foundation/01-04-SUMMARY.md`
</output>
