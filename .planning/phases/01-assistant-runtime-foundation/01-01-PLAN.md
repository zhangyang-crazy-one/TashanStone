---
phase: 01-assistant-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types.ts
  - src/services/assistant-runtime/types.ts
  - src/services/assistant-runtime/settingsCatalog.ts
  - src/services/assistant-runtime/defaults.ts
  - utils/translations.ts
  - test/services/assistantRuntime.contracts.test.ts
  - test/services/assistantSettingsCatalog.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "Maintainers can point to one assistant runtime contract that is independent of the current chat-panel hook."
    - "The Phase 01 settings schema mirrors the locked operator versus notebook surface split."
    - "Operator settings descriptors include `plugins` and `skills` as separate sections instead of folding them into `tools` or `agents`."
    - "New settings labels and shortcut metadata resolve through bilingual descriptors instead of hardcoded UI strings."
  artifacts:
    - path: "src/services/assistant-runtime/types.ts"
      provides: "Shared runtime request, caller, session, notebook-input, adapter, event, and result contracts"
    - path: "src/services/assistant-runtime/settingsCatalog.ts"
      provides: "Translation-backed descriptor catalog for operator and notebook settings surfaces"
    - path: "test/services/assistantSettingsCatalog.test.ts"
      provides: "Regression coverage for the settings information architecture and translation-backed metadata"
  key_links:
    - from: "src/services/assistant-runtime/types.ts"
      to: "src/app/hooks/useAIWorkflow.ts"
      via: "runtime contracts exclude React setters and UI-owned message arrays"
      pattern: "AssistantRuntime(Request|Event|Result)"
    - from: "src/services/assistant-runtime/settingsCatalog.ts"
      to: "utils/translations.ts"
      via: "descriptor ids resolve through translation keys rather than literal display strings"
      pattern: "assistantSettings\\."
---

<objective>
Define the Phase 01 contracts first so runtime extraction and settings alignment happen against stable interfaces instead of hook-owned state or ad hoc section ids.

Purpose: Lock the shared assistant runtime boundary and the TUI-driven settings schema per D-01 through D-08 and the user's Plugins/Skills correction.
Output: Runtime contracts, descriptor-backed defaults, and contract tests that downstream plans can implement against.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
@types.ts
@src/app/hooks/useAIWorkflow.ts
@components/AISettingsModal.tsx
@electron/database/repositories/configRepository.ts
@utils/translations.ts

<interfaces>
From [types.ts](/home/zhangyangrui/my_programes/TashanStone/types.ts):
```typescript
export interface AIConfig {
  provider: AIProvider;
  model: string;
  temperature: number;
  language: 'en' | 'zh';
  contextEngine?: {
    enabled: boolean;
    maxTokens: number;
    compactThreshold: number;
    pruneThreshold: number;
    truncateThreshold: number;
    messagesToKeep: number;
    checkpointInterval: number;
  };
  assistantSettings?: {
    surface: 'operator' | 'notebook';
    sectionBySurface: Record<'operator' | 'notebook', string>;
  };
}
```

From [components/AISettingsModal.tsx](/home/zhangyangrui/my_programes/TashanStone/components/AISettingsModal.tsx):
```typescript
type Tab = 'ai' | 'appearance' | 'prompts' | 'mcp' | 'keyboard' | 'security' | 'context';
```

From [src/app/hooks/useAIWorkflow.ts](/home/zhangyangrui/my_programes/TashanStone/src/app/hooks/useAIWorkflow.ts):
```typescript
interface UseAIWorkflowOptions {
  aiConfig: AIConfig;
  chatMessages: ChatMessage[];
  setChatMessages: Dispatch<SetStateAction<ChatMessage[]>>;
  filesRef: MutableRefObject<MarkdownFile[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared assistant runtime contract surface</name>
  <files>types.ts, src/services/assistant-runtime/types.ts, test/services/assistantRuntime.contracts.test.ts</files>
  <read_first>
    - .planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
    - .planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
    - types.ts
    - src/app/hooks/useAIWorkflow.ts
    - services/aiService.ts
  </read_first>
  <behavior>
    - Test 1: A runtime request describes caller identity, session reference, model config, transport metadata, and notebook context without importing React-only state.
    - Test 2: Runtime events cover lifecycle, streaming deltas, tool status, success, and error through one caller-neutral envelope.
    - Test 3: The public contract surface can be re-exported from `types.ts` without dragging UI hook types into the runtime layer.
  </behavior>
  <action>Per D-01, D-02, and D-03, create the shared contract layer in `src/services/assistant-runtime/types.ts` and expose the cross-surface types from `types.ts`. Define serializable contracts for `AssistantRuntimeRequest`, `AssistantSessionRef`, `AssistantRuntimeCaller`, `AssistantNotebookContextInput`, `AssistantContextAdapter`, `AssistantRuntimeEvent`, `AssistantRuntimeToolInvocation`, and `AssistantRuntimeResult`. Do not include React setters, `Dispatch`, JSX, DOM handles, or hook-local message arrays. Add a focused contract test file that fails if the discriminated unions or stable field names drift back toward UI-owned orchestration.</action>
  <acceptance_criteria>
    - `rg -n "AssistantRuntime(Request|Event|Result)|AssistantContextAdapter|AssistantNotebookContextInput" types.ts src/services/assistant-runtime/types.ts`
    - `bun run vitest run test/services/assistantRuntime.contracts.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantRuntime.contracts.test.ts</automated>
  </verify>
  <done>The repository exposes one caller-neutral runtime contract surface, and regression tests guard it from slipping back into hook-specific types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the TUI-aligned settings descriptor catalog</name>
  <files>src/services/assistant-runtime/settingsCatalog.ts, src/services/assistant-runtime/defaults.ts, utils/translations.ts, test/services/assistantSettingsCatalog.test.ts</files>
  <read_first>
    - .planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
    - .planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
    - components/AISettingsModal.tsx
    - utils/translations.ts
    - types.ts
  </read_first>
  <behavior>
    - Test 1: The catalog exposes two top-level surfaces, `operator` and `notebook`, matching D-05.
    - Test 2: Operator sections explicitly include `runtime`, `models`, `fallback`, `tools`, `plugins`, `skills`, `agents`, `scheduling`, `channels`, `media`, `safety`, `observability`, `ui`, `keyboard`, and `about`.
    - Test 3: Notebook sections explicitly include `workspace`, `editor`, `preview`, `links-tags`, `graph`, `search-index`, `study-srs`, `appearance`, `shortcuts`, `voice-ocr`, `backup`, and `about`.
    - Test 4: Labels, help text, phase metadata, and shortcuts resolve through bilingual translation keys or structured metadata instead of literal strings.
  </behavior>
  <action>Per D-04, D-05, D-06, D-07, D-08, and D-11, encode the current TUI settings information architecture as typed descriptors and defaults. Treat the user's correction as locked: create `plugins` and `skills` as separate operator section ids and descriptor entries, not aliases of `tools` or `agents`. Back every new title/help/shortcut with translation keys in `utils/translations.ts`. Mark deferred sections as data-only so executors do not accidentally implement full settings pages in Phase 01, per D-09 and D-12.</action>
  <acceptance_criteria>
    - `rg -n "id: 'plugins'|id: 'skills'|AssistantSettingsSurfaceId|phase: 'deferred'" src/services/assistant-runtime/settingsCatalog.ts`
    - `bun run vitest run test/services/assistantSettingsCatalog.test.ts`
    - `bun run tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantSettingsCatalog.test.ts && bun run tsc --noEmit</automated>
  </verify>
  <done>The design contract is captured in typed, bilingual descriptors with explicit Plugins and Skills sections, and later plans can consume it without inventing section ids or labels.</done>
</task>

</tasks>

<verification>
Confirm the new runtime contracts contain no React imports and that the settings catalog exposes both surfaces with explicit operator `plugins` and `skills` sections backed by translation keys.
</verification>

<success_criteria>
Phase 01 starts with a stable runtime contract layer and a corrected settings schema that match the locked TUI direction and can support downstream implementation without reinterpretation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-assistant-runtime-foundation/01-assistant-runtime-foundation-01-SUMMARY.md`
</output>
