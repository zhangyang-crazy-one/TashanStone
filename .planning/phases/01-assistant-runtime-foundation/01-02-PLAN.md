---
phase: 01-assistant-runtime-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/services/assistant-runtime/index.ts
  - src/services/assistant-runtime/createAssistantRuntime.ts
  - src/services/assistant-runtime/contextAssembler.ts
  - src/services/assistant-runtime/providerExecution.ts
  - services/aiService.ts
  - services/ai/aiStreamingPipeline.ts
  - test/services/assistantRuntime.execution.test.ts
  - test/services/assistantRuntime.context.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "A maintainer can invoke assistant execution through a shared runtime entrypoint instead of the current UI-owned path."
    - "The same runtime interface works across supported providers while streaming through one event model."
    - "Notebook context assembly happens through caller-provided adapters rather than direct React or storage reads."
  artifacts:
    - path: "src/services/assistant-runtime/createAssistantRuntime.ts"
      provides: "Runtime factory and async event-stream execution boundary"
    - path: "src/services/assistant-runtime/contextAssembler.ts"
      provides: "Adapter-driven notebook context assembly using existing context primitives"
    - path: "test/services/assistantRuntime.execution.test.ts"
      provides: "Regression coverage for runtime lifecycle, streaming, and provider execution"
  key_links:
    - from: "src/services/assistant-runtime/createAssistantRuntime.ts"
      to: "services/aiService.ts"
      via: "provider execution wraps existing AI service entrypoints instead of rebuilding provider clients"
      pattern: "generateAIResponse|generateAIResponseStream"
    - from: "src/services/assistant-runtime/contextAssembler.ts"
      to: "src/services/context/index.ts"
      via: "context injection and budget management use the existing context subsystem"
      pattern: "ContextInjector|DEFAULT_CONTEXT_BUDGET_RATIOS|formatProjectContextForInjection"
---

<objective>
Implement the runtime core behind the new contract so provider execution, streaming, and context assembly no longer belong to the chat-panel hook path.

Purpose: Satisfy CORE-01 through CORE-04 with a real shared runtime service layer per D-01, D-02, and D-03.
Output: Runtime factory, provider execution wrapper, adapter-driven context assembler, and regression tests for runtime behavior.
</objective>

<execution_context>
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/my_programes/TashanStone/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
@.planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
@services/aiService.ts
@services/ai/aiStreamingPipeline.ts
@src/services/context/index.ts
@src/app/hooks/useAIWorkflow.ts
@src/services/assistant-runtime/types.ts

<interfaces>
From [services/aiService.ts](/home/zhangyangrui/my_programes/TashanStone/services/aiService.ts):
```typescript
export const generateAIResponse = async (
  prompt: string,
  config: AIConfig,
  systemInstruction?: string,
  jsonMode?: boolean,
  contextFiles?: MarkdownFile[],
  toolsCallback?: ToolCallback,
  retrievedContext?: string,
  conversationHistory?: ChatMessage[],
  disableTools?: boolean,
  toolEventCallback?: ToolEventCallback
): Promise<string> => { ... }

export { generateAIResponseStream } from "./ai/aiStreamingPipeline";
```

From [src/services/context/index.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/context/index.ts):
```typescript
export {
  ContextInjector,
  DEFAULT_CONTEXT_BUDGET_RATIOS,
  formatProjectContextForInjection,
} from './injector';
```

From [src/services/assistant-runtime/types.ts](/home/zhangyangrui/my_programes/TashanStone/src/services/assistant-runtime/types.ts):
```typescript
export interface AssistantRuntimeRequest {
  requestId: string;
  session: AssistantSessionRef;
  caller: AssistantRuntimeCaller;
  modelConfig: AIConfig;
  input: AssistantRuntimeInput;
  notebook?: AssistantNotebookContextInput;
  transport?: AssistantRuntimeTransportMetadata;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the shared runtime entrypoint and event stream</name>
  <files>src/services/assistant-runtime/index.ts, src/services/assistant-runtime/createAssistantRuntime.ts, src/services/assistant-runtime/providerExecution.ts, services/aiService.ts, services/ai/aiStreamingPipeline.ts, test/services/assistantRuntime.execution.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/types.ts
    - services/aiService.ts
    - services/ai/aiStreamingPipeline.ts
    - src/app/hooks/useAIWorkflow.ts
    - .planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: A caller executes one runtime request and receives lifecycle, stream-delta, tool-status, and result events without passing UI setters.
    - Test 2: Provider switching keeps the same runtime event envelope and completion contract.
    - Test 3: Runtime tool-status emission is owned by the runtime layer rather than hook-local orchestration.
  </behavior>
  <action>Per D-01 and D-02, build `createAssistantRuntime(...)` and the provider execution seam under `src/services/assistant-runtime/`. Reuse `generateAIResponse`, `generateAIResponseStream`, and the existing tool plumbing instead of introducing a second provider abstraction, as required by research. The runtime must own request normalization, stream/event emission, and terminal result handling. Do not leak provider-specific response formats, React setters, or component-local state into the runtime API.</action>
  <acceptance_criteria>
    - `rg -n "createAssistantRuntime|AsyncGenerator|tool-status|stream-delta" src/services/assistant-runtime`
    - `bun run vitest run test/services/assistantRuntime.execution.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantRuntime.execution.test.ts</automated>
  </verify>
  <done>The repository has one shared runtime entrypoint with a stable event stream, and callers no longer need to own provider orchestration themselves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add adapter-driven notebook context assembly</name>
  <files>src/services/assistant-runtime/contextAssembler.ts, src/services/assistant-runtime/createAssistantRuntime.ts, test/services/assistantRuntime.context.test.ts</files>
  <read_first>
    - src/services/assistant-runtime/types.ts
    - src/services/context/index.ts
    - src/app/hooks/useAIWorkflow.ts
    - .planning/phases/01-assistant-runtime-foundation/01-CONTEXT.md
    - .planning/phases/01-assistant-runtime-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Notebook notes, workspace state, and knowledge payloads can be assembled from caller-provided adapters into one runtime prompt path.
    - Test 2: The context assembler does not read React hook state, browser localStorage, or chat-panel internals directly.
    - Test 3: At least two caller shapes in tests can use the same runtime request/context path without duplicating orchestration logic.
  </behavior>
  <action>Per D-03 and D-12, implement `contextAssembler.ts` as the runtime-owned context boundary on top of the existing context subsystem. Accept notebook/workspace/knowledge data through `AssistantContextAdapter` implementations and use `ContextInjector` plus the current context budget helpers to shape the final prompt. Keep channel adapters, multimodal normalization, and full parity work out of scope in this phase. The goal here is a reusable adapter contract, not future-scope feature delivery.</action>
  <acceptance_criteria>
    - `rg -n "AssistantContextAdapter|ContextInjector|formatProjectContextForInjection" src/services/assistant-runtime/contextAssembler.ts`
    - `bun run vitest run test/services/assistantRuntime.context.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>bun run vitest run test/services/assistantRuntime.context.test.ts</automated>
  </verify>
  <done>The runtime can assemble notebook-native context through adapters, and tests prove the same contract works for more than one caller shape without UI ownership.</done>
</task>

</tasks>

<verification>
Run the runtime execution and context tests, then confirm the runtime now owns execution semantics while callers only provide request data and render events.
</verification>

<success_criteria>
Phase 01 has a real shared runtime core that streams through one interface, wraps existing provider paths, and assembles notebook context through adapters instead of UI-owned state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-assistant-runtime-foundation/01-02-SUMMARY.md`
</output>
